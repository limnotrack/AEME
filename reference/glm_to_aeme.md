# Load a GLM nml file and convert to aeme object

Load a GLM nml file and convert to aeme object

## Usage

``` r
glm_to_aeme(nml_file)
```

## Arguments

- nml_file:

  Path to GLM nml file

## Value

Aeme object
