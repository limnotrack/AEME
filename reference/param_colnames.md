# Get AEME parameters data frame column names

Get AEME parameters data frame column names

## Usage

``` r
param_colnames(incl_opt = TRUE)
```

## Arguments

- incl_opt:

  Logical; if TRUE, include optional parameter names. Default is TRUE.

## Value

A character vector of AEME parameter data frame column names.
