# Get model version

Get model version

## Usage

``` r
get_model_version(model)
```

## Arguments

- model:

  model name. Only "glm_aed", "gotm_wet", and "dy_cd" are supported.

## Value

version string
