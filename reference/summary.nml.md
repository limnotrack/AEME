# Summarise an nml object

Currently delegates to
[`print.nml`](https://limnotrack.com/reference/print.nml.md).

## Usage

``` r
summary.nml(object, ...)
```

## Arguments

- object:

  An nml object.

- ...:

  Further arguments passed to `print.nml`.

## Value

`object`, invisibly.
