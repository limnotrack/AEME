# Check if object is a valid Aeme object

Check if object is a valid Aeme object

## Usage

``` r
check_aeme(aeme)
```

## Arguments

- aeme:

  object to check

## Value

Invisibly returns the Aeme object if valid, otherwise throws an error.
