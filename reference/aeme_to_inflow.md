# Convert AEME model outputs to inflow format

Convert AEME model outputs to inflow format

## Usage

``` r
aeme_to_inflow(aeme)
```

## Arguments

- aeme:

  Aeme object.

## Value

List of data frames with inflow variables for each model
