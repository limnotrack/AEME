% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_initial_conditions.R
\name{get_initial_conditions}
\alias{get_initial_conditions}
\title{Get the stored initial-conditions specification}
\usage{
get_initial_conditions(aeme, model = NULL)
}
\arguments{
\item{aeme}{Aeme object.}

\item{model}{character(1); optional model name. If supplied, the resolved
(merged) initial conditions for that model are returned instead of the
full specification.}
}
\value{
A list. When \code{model} is \code{NULL}, the stored specification (a list
with a \code{default} element and an element per model with overrides), or
\code{NULL} if none has been set. When \code{model} is supplied, a list with any
of \code{depth}, \code{profile} and \code{wq}.
}
\description{
Return the structured initial-conditions specification set by
\code{\link[=set_initial_conditions]{set_initial_conditions()}}, or, for a single \code{model}, that model's
resolved initial conditions (its overrides merged over the generic
defaults).
}
\examples{
aeme_file <- system.file("extdata/aeme.rds", package = "AEME")
aeme <- readRDS(aeme_file)
aeme <- set_initial_conditions(aeme, depth = 10)
get_initial_conditions(aeme)
get_initial_conditions(aeme, model = "glm_aed")
}
\seealso{
\code{\link[=set_initial_conditions]{set_initial_conditions()}}
}
