% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_gotm_inflows.R
\name{set_gotm_inflows}
\alias{set_gotm_inflows}
\title{Set inflow data for a GOTM-WET simulation directory}
\usage{
set_gotm_inflows(
  path_gotm,
  inf_list,
  inf_factor = 1,
  use_bgc = NULL,
  yaml_file = file.path(path_gotm, "gotm.yaml")
)
}
\arguments{
\item{path_gotm}{filepath; to GOTM-WET directory (containing \code{gotm.yaml}
and an \verb{inputs/} subdirectory)}

\item{inf_list}{named list of data.frames, one per inflow. Each must have
a \code{Date} column plus flow/temperature/salt columns (e.g. \code{HYD_flow},
\code{HYD_temp}, \code{CHM_salt}) -- see \code{\link[=add_inflow]{add_inflow()}} for the expected schema.}

\item{inf_factor}{numeric; scaling factor applied to all inflow flow
rates. Default is \code{1}.}

\item{use_bgc}{logical; also write BGC concentration inflow files.
Defaults to the existing \code{fabm.use} setting in \code{gotm.yaml}.}

\item{yaml_file}{filepath; path to the \code{gotm.yaml} file to update.
Defaults to \code{gotm.yaml} in \code{path_gotm}.}
}
\value{
invisibly, the updated yaml list
}
\description{
Thin, \code{aeme}-free wrapper around the internal inflow writer used by
\code{\link[=build_aeme]{build_aeme()}}. Writes \verb{inputs/inf_\{flow,temp,salt\}_<name>.dat} (and, if
BGC is coupled, \verb{inputs/inf_chem_<name>.dat}) per inflow into
\code{path_gotm}, and updates the \code{streams} block of \code{gotm.yaml} to point at
them. Existing stream entries not named in \code{inf_list} are left untouched.
}
\examples{
\dontrun{
set_gotm_inflows(path_gotm, inf_list = list(stream1 = inflow_df))
}
}
