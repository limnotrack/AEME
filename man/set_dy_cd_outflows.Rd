% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_dy_cd_outflows.R
\name{set_dy_cd_outflows}
\alias{set_dy_cd_outflows}
\title{Set outflow data for a DYRESM-CAEDYM simulation directory}
\usage{
set_dy_cd_outflows(
  path_dy,
  outf,
  heights_wdr,
  outf_factor = 1,
  update_stg = TRUE
)
}
\arguments{
\item{path_dy}{filepath; the \code{dy_cd} model directory (containing the
\verb{<lakename>.stg} file).}

\item{outf}{named list of data.frames, one per outflow, each with a
\code{Date} column and a \code{HYD_flow} column -- see \code{\link[=add_outflows]{add_outflows()}} for the
expected schema.}

\item{heights_wdr}{named numeric vector; outlet elevation (m ASL) for
each name in \code{outf}.}

\item{outf_factor}{numeric; scaling factor applied to all outflow flow
rates. Default is \code{1}.}

\item{update_stg}{logical; also rebuild the outlet block of
\verb{<lakename>.stg} with \code{heights_wdr}. Default \code{TRUE}.}
}
\value{
invisibly, \code{NULL}.
}
\description{
Thin, \code{aeme}-free wrapper around the internal outflow writer used by
\code{\link[=build_aeme]{build_aeme()}}. Writes \verb{<lakename>.wdr} into \code{path_dy} and, so the outlet
set stays consistent, rebuilds the outlet block of \verb{<lakename>.stg} with
the supplied heights.
}
\details{
DYRESM-CAEDYM's \code{.wdr} writer (\code{make_dy_wdr()}) only supports a single
outflow series (or the internal water-balance \code{outflow}/\code{wbal} pair), so
\code{outf} should normally hold exactly one data.frame.
}
\examples{
\dontrun{
set_dy_cd_outflows(path_dy, outf = list(outflow = outflow_df),
                   heights_wdr = c(outflow = 12.07))
}
}
