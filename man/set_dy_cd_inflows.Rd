% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_dy_cd_inflows.R
\name{set_dy_cd_inflows}
\alias{set_dy_cd_inflows}
\title{Set inflow data for a DYRESM-CAEDYM simulation directory}
\usage{
set_dy_cd_inflows(path_dy, list_inf, inf_factor = 1, update_stg = TRUE)
}
\arguments{
\item{path_dy}{filepath; the \code{dy_cd} model directory (containing the
\verb{<lakename>.stg} file).}

\item{list_inf}{named list of data.frames, one per inflow. Each must have
a \code{Date} column plus flow/temperature/salt columns (e.g. \code{HYD_flow},
\code{HYD_temp}, \code{CHM_salt}) -- see \code{\link[=add_inflow]{add_inflow()}} for the expected schema.
Column names are translated to DYRESM-CAEDYM's own via
\code{rename_modelvars()}.}

\item{inf_factor}{numeric; scaling factor applied to all inflow flow
rates. Default is \code{1}.}

\item{update_stg}{logical; also rebuild the inflow block of
\verb{<lakename>.stg} to match \code{names(list_inf)}. Default \code{TRUE}.}
}
\value{
invisibly, \code{NULL}.
}
\description{
Thin, \code{aeme}-free wrapper around the internal inflow writer used by
\code{\link[=build_aeme]{build_aeme()}}. Writes \verb{<lakename>.inf} into \code{path_dy} and, so the
inflow set stays consistent, rebuilds the inflow block of
\verb{<lakename>.stg} (see \code{.rewrite_dy_stg()} note about inflow geometry
defaults).
}
\examples{
\dontrun{
set_dy_cd_inflows(path_dy, list_inf = list(FWMT = inflow_df))
}
}
