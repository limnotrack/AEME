% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_model_output.R
\name{plot_model_output}
\alias{plot_model_output}
\title{Plot a variable from model output -- an \code{Aeme} object or a raw output list}
\usage{
plot_model_output(
  x,
  var_sim,
  model = NULL,
  ens_n = 1,
  remove_spin_up = TRUE,
  var_lims = NULL,
  ylim = NULL
)
}
\arguments{
\item{x}{either an \code{Aeme} object, or a list as returned by
\code{\link[=read_glm_output]{read_glm_output()}}/\code{\link[=read_gotm_output]{read_gotm_output()}}/\code{\link[=read_simstrat_output]{read_simstrat_output()}}/
\code{\link[=read_model_outputs]{read_model_outputs()}}.}

\item{var_sim}{character; name of the variable to plot, as it appears in
\code{names(x)} (or in the model output list, if \code{x} is an \code{Aeme} object) --
a \code{var_aeme} name if \link{key_naming} has a translation for it, otherwise
its raw model/netCDF name.}

\item{model}{character; model to plot, when \code{x} is an \code{Aeme} object with
more than one model. Ignored (and unnecessary) when \code{x} is already a
raw output list. Defaults to the first model in \code{\link[=list_models]{list_models()}} if not
supplied.}

\item{ens_n}{integer; ensemble member to plot, when \code{x} is an \code{Aeme}
object. Default \code{1}.}

\item{remove_spin_up}{logical; when \code{x} is an \code{Aeme} object, drop the
spin-up period before plotting (see \code{\link[=get_date_index]{get_date_index()}}). Default \code{TRUE}.
Ignored when \code{x} is already a raw output list -- trim it yourself first
if needed.}

\item{var_lims}{numeric vector of length 2; colour scale limits for a
depth x time plot. Default \code{NULL} (ranged to the data).}

\item{ylim}{numeric vector of length 2; y-axis limits for a line plot.
Default \code{NULL} (ranged to the data).}
}
\value{
A ggplot2 object (the long-format data frame instead, with a
warning, for a grouped variable that has no \code{Date} dimension at all).
}
\description{
A lightweight ggplot2 plotting helper that works either directly on the
list returned by \code{\link[=read_glm_output]{read_glm_output()}}, \code{\link[=read_gotm_output]{read_gotm_output()}},
\code{\link[=read_simstrat_output]{read_simstrat_output()}}, or \code{\link[=read_model_outputs]{read_model_outputs()}} -- for when you don't
have (or don't want to build) a full \code{Aeme} object -- or on an \code{Aeme}
object itself, in which case \code{\link[=get_var]{get_var()}}/\code{\link[=plot_var]{plot_var()}} do the work for
you. Dispatches on the shape of the requested variable: a depth x time
tile plot for \verb{(z, time)} variables (same rendering as \code{\link[=plot_output]{plot_output()}}'s
default backend), a simple time series for \code{(time)}-only variables (e.g.
evaporation), and a line plot (one line per combination of its non-time
dimensions) for a \code{\link[=new_grouped_var]{new_grouped_var()}} variable, i.e. one with dimensions
other than \code{(time)}/\verb{(z, time)} -- e.g. one line per sediment zone for a
GLM-AED \verb{(nzones, time)} AED flux variable.
}
\details{
For observation overlay, faceting across multiple models/variables at
once, or the base-graphics backend, use \code{\link[=plot_output]{plot_output()}} instead -- this
function is intentionally minimal, and does not (yet) support
\code{aeme_grouped_var} variables when called on an \code{Aeme} object through
\code{\link[=plot_output]{plot_output()}}/\code{\link[=plot_output_base]{plot_output_base()}}.
}
\examples{
\dontrun{
# On a raw output list
out <- read_glm_output(file = outfile)
plot_model_output(out, "HYD_temp")        # depth x time tile plot
plot_model_output(out, "LKE_lvlwtr")      # simple time series
plot_model_output(out, "SDF_Fsed_oxy_Z")  # one line per sediment zone

# On an Aeme object directly
plot_model_output(aeme, "HYD_temp", model = "glm_aed")
}
}
