% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag_aeme.R
\name{diag_heat_budget}
\alias{diag_heat_budget}
\title{Whole-lake heat budget vs observations: net deficit/surplus, or just
surface/bottom redistribution?}
\usage{
diag_heat_budget(
  aeme,
  model,
  depth_breaks = c(-0.01, 2, 6, 10, 15, 20, 100),
  bias_ok_degC = 0.5,
  redistribution_ratio = 0.5
)
}
\arguments{
\item{aeme}{Aeme object; already built and run for \code{model}.}

\item{model}{character; single model code to diagnose (see
\code{\link{check_model}}).}

\item{depth_breaks}{numeric vector; passed to \code{\link{cut}} for the
by-depth-band summary.}

\item{bias_ok_degC}{numeric; below this absolute volume-weighted bias,
classified \code{"ok"}.}

\item{redistribution_ratio}{numeric; if the volume-weighted bias's
magnitude is below this fraction of the largest single-band absolute
bias, the bands are judged to be substantially cancelling each other out
(heat misplaced, not missing/excess) rather than agreeing (a genuine net
deficit or surplus).}
}
\value{
list with \code{volume_weighted_bias_degC}, \code{total_bias_J},
\code{lake_volume_m3}, \code{by_depth_band} (data frame), \code{classification} (one of
\code{"deficit"}, \code{"surplus"}, \code{"redistribution"}, \code{"ok"}, \code{"no_data"}),
\code{detail}.
}
\description{
Uses \code{HYD_temp} (not \code{HYD_nrgcnt} directly - there is no independent
observation of energy density to compare against, only temperature) for
the bias itself, volume-weighted via the lake's own hypsography, then
reports the implied total heat-content bias in Joules for an intuitive
magnitude (\code{rho * cw * volume-weighted-bias * lake_volume}).
}
