% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_dy_cd_param.R
\name{set_dy_cd_param}
\alias{set_dy_cd_param}
\title{Set one or more parameter values in a DYRESM-CAEDYM configuration}
\usage{
set_dy_cd_param(
  path_dy,
  ...,
  cfg_file = find_dy_cd_cfg(path_dy),
  par_file = file.path(path_dy, "dyresm3p1.par")
)
}
\arguments{
\item{path_dy}{filepath; directory containing the DYRESM-CAEDYM
configuration (the \code{dy_cd} model directory).}

\item{...}{named parameter/value pairs to set, e.g. \code{Kw = 0.5},
\code{max_layer_thickness = 2}, \code{eta_S = 0.5}. See Details for accepted
names.}

\item{cfg_file}{filepath; the \verb{<lakename>.cfg} file to edit. Defaults to
the one found in \code{path_dy} via \code{\link[=find_dy_cd_cfg]{find_dy_cd_cfg()}}.}

\item{par_file}{filepath; the \code{dyresm3p1.par} file to edit. Defaults to
\code{dyresm3p1.par} in \code{path_dy}.}
}
\value{
invisibly, a named list of the values that were set.
}
\description{
Thin, \code{aeme}-free wrapper for editing a DYRESM-CAEDYM model directory in
place. DYRESM-CAEDYM has no keyed config file like GLM-AED's \code{.nml} --
its tunables are split between the positional \verb{<lakename>.cfg}
(light extinction, layer thickness limits, time step, output interval)
and the positional \code{dyresm3p1.par} (bulk aerodynamic drag, albedo,
emissivity, mixing efficiencies, ...). This function exposes a curated
set of those by friendly name and edits the right line of the right
file, leaving formatting otherwise untouched. Intended for a
DYRESM-CAEDYM-only workflow where a user just wants to tweak parameters,
run the model, and load the output.
}
\details{
Setting \code{Kw} also updates the \code{PAR} line of \code{caedym3p1.bio} when that
file is present, mirroring \code{\link[=build_aeme]{build_aeme()}}'s own behaviour so a BGC run
stays consistent.

Accepted names:
\describe{
\item{\verb{<lakename>.cfg}}{\code{start_date}, \code{sim_days}, \code{run_caedym},
\code{output_interval}, \code{Kw}, \code{min_layer_thickness}, \code{max_layer_thickness},
\code{timestep}}
\item{\code{dyresm3p1.par}}{\code{drag_coef}, \code{albedo}, \code{emissivity},
\code{crit_wind_speed}, \code{output_time}, \code{bubbler_entrain_coef},
\code{plume_entrain_coef}, \code{eta_K}, \code{eta_P}, \code{eta_S}, \code{eff_surf_area_coef},
\code{bbl_dissip_coef}, \code{vert_mix_coef}}
}
}
\examples{
\dontrun{
set_dy_cd_param(path_dy, Kw = 0.5, max_layer_thickness = 2)
set_dy_cd_param(path_dy, eta_K = 0.1, eta_P = 0.3, eta_S = 0.5)
}
}
