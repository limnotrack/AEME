% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_model_test.R
\name{run_model_test}
\alias{run_model_test}
\title{Run a single what-if model scenario from a path alone}
\usage{
run_model_test(
  model,
  path,
  param_overrides = list(),
  init = list(),
  inflow_args = NULL,
  outflow_args = NULL,
  tgt_vars = NULL,
  verbose = FALSE,
  safe = TRUE
)
}
\arguments{
\item{model}{character; one of \code{"glm_aed"}, \code{"gotm_wet"},
\code{"simstrat_aed"}, \code{"simstrat_aed2"}.}

\item{path}{filepath; directory containing the existing model
configuration to edit and run (as already written by \code{\link[=build_aeme]{build_aeme()}}).}

\item{param_overrides}{named list of parameter/value pairs, forwarded to
the model's \verb{set_*_param()} wrapper, e.g. \code{list(Kw = 0.5)} for GLM-AED
or \code{list(`time.dt` = 1800)} for GOTM-WET. Empty list (default)
leaves parameters unchanged.}

\item{init}{named list forwarded to the model's \verb{set_*_init()} wrapper,
e.g. \code{list(temp = seq(20, 10, length.out = 10), wq_init = list(NIT_amm = 0.5))}. Empty list (default) leaves initial conditions
unchanged.}

\item{inflow_args}{named list forwarded to the model's \verb{set_*_inflows()}
wrapper; must include \code{data}, a named list of inflow data.frames (see
\code{\link[=add_inflow]{add_inflow()}}). \code{NULL} (default) leaves inflows unchanged.}

\item{outflow_args}{named list forwarded to the model's
\verb{set_*_outflows()} wrapper; must include \code{data}, a named list of
outflow data.frames (see \code{\link[=add_outflows]{add_outflows()}}), plus any further arguments
the model requires (e.g. \code{heights_wdr}, and \code{surface_elev} for
Simstrat). \code{NULL} (default) leaves outflows unchanged.}

\item{tgt_vars}{character vector of output variables to read back (passed
as \code{vars_sim} to the model's \verb{read_*_output()} wrapper). \code{NULL}
(default) reads every variable the reader returns by default.}

\item{verbose}{logical; passed through to the model's \verb{run_*()} wrapper.}

\item{safe}{logical; if \code{TRUE} (default), a failed edit or model run is
caught and reported with \code{message()} instead of stopping -- useful when
looping over many scenarios and one bad combination shouldn't abort the
rest. Set \code{FALSE} to let errors propagate normally.}
}
\value{
A list of the requested output variables (as returned by the
model's \verb{read_*_output()} wrapper), or \code{NULL} if \code{safe = TRUE} and the
edit/run/read failed.
}
\description{
Generic driver for quickly testing different parameters, nml/yaml/par
settings, inflow/outflow concentrations, or initial conditions against an
existing GLM-AED, GOTM-WET, Simstrat-AED or Simstrat-AED2 configuration
directory, without needing an \code{aeme} object. Dispatches to each model's
thin path-based wrapper functions (e.g. \code{\link[=set_glm_param]{set_glm_param()}}/
\code{\link[=set_glm_init]{set_glm_init()}}/\code{\link[=set_glm_inflows]{set_glm_inflows()}}/\code{\link[=set_glm_outflows]{set_glm_outflows()}}/
\code{\link[=run_glm_aed]{run_glm_aed()}}/\code{\link[=read_glm_output]{read_glm_output()}} for \code{model = "glm_aed"}) so scenario
code can be written once and pointed at any supported model.
}
\details{
Each \verb{*_args} list is applied via \code{do.call()} against the corresponding
thin wrapper, with \code{path} supplied automatically as its first argument --
so the names inside each list must match that wrapper's own arguments.
\code{inflow_args}/\code{outflow_args} are the exception: their data.frame list
argument is always named \code{data} (regardless of the underlying wrapper's
own naming -- \code{list_inf}/\code{inf_list}/\code{inf}), and any extra arguments the
underlying outflow writer requires (e.g. Simstrat's \code{heights_wdr}/
\code{surface_elev}) are passed alongside it by name.
}
\examples{
\dontrun{
run_model_test("glm_aed", path_glm, param_overrides = list(Kw = 0.8),
               tgt_vars = "HYD_temp")
run_model_test("simstrat_aed2", path_simstrat,
               init = list(wq_init = list(NIT_amm = 0.5)),
               tgt_vars = "HYD_temp")
}
}
