% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_simstrat_aed2.R
\name{install_simstrat_aed2}
\alias{install_simstrat_aed2}
\title{Install a Simstrat-AED2 executable for AEME}
\usage{
install_simstrat_aed2(
  version = "latest",
  os = NULL,
  repo = "limnotrack/AEME",
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{version}{Character. The Simstrat version to install, e.g.
\code{"3.0.4"}. Use \code{\link[=list_simstrat_aed2_versions]{list_simstrat_aed2_versions()}} to see what's available.
Defaults to \code{"latest"}, which resolves to the highest version number
available for the current platform.}

\item{os}{Character. One of \code{"windows"}, \code{"macos"}, or \code{"linux"}.
Defaults to the platform R is currently running on; you shouldn't
normally need to set this. Note that only a Windows build is currently
published as a release asset -- \code{macos}/\code{linux} release assets don't
exist yet, so those platforms will report no versions available until
binaries are published.}

\item{repo}{Character. The \code{"owner/repo"} GitHub repository that Simstrat
binaries are attached to. Defaults to \code{"limnotrack/AEME"}.}

\item{force}{Logical. If \code{FALSE} (the default) and this version is
already installed for this platform, the download/verification steps
are skipped and the existing path is returned. Set to \code{TRUE} to
re-download and reinstall anyway.}

\item{quiet}{Logical. If \code{TRUE}, suppresses progress messages (download
errors and checksum failures still raise, and are never silenced).}
}
\value{
Invisibly, the file path to the installed Simstrat-AED2
executable.
}
\description{
Downloads a pre-compiled Simstrat (coupled with AED2) binary for the
current platform, verifies it against its published SHA256 checksum, and
installs it into a persistent user cache directory. Binaries are attached
as assets to AEME's GitHub releases, with the Simstrat version encoded in
the asset filename (e.g. \verb{simstrat-windows-3.0.4.zip}), mirroring
\code{\link[=install_glm_aed]{install_glm_aed()}}. Because a given Simstrat version isn't tied to any
one AEME release, this function searches across all releases of \code{repo} to
find the release that has the requested version's asset attached.
}
\details{
Binaries are cached under \code{\link[tools:R_user_dir]{tools::R_user_dir("AEME", "data")}},
in an \verb{<os>/<version>/} subdirectory (the same root \code{\link[=install_glm_aed]{install_glm_aed()}}
uses -- the two coexist there because the installed files themselves are
named differently, \code{simstrat}/\code{simstrat.exe} vs \code{glm}/\code{glm.exe}). Multiple
Simstrat versions can be installed side by side and switched between via
the \code{version} argument to \code{\link[=simstrat_aed2_exe_path]{simstrat_aed2_exe_path()}} without
re-downloading.

Every binary is published alongside a \code{.sha256} checksum file in the same
release. This function downloads both, recomputes the SHA256 of the
downloaded zip locally, and compares it to the published value before
extracting anything. If the checksums don't match, or if no checksum file
is found at all, installation is aborted and nothing is extracted -- this
function will not install an unverified binary under any circumstances.
}
\examples{
\dontrun{
install_simstrat_aed2(version = "3.0.4")

# Force re-download and reinstall
install_simstrat_aed2(version = "3.0.4", force = TRUE)
}

}
\seealso{
\code{\link[=list_simstrat_aed2_versions]{list_simstrat_aed2_versions()}} to discover available versions,
\code{\link[=simstrat_aed2_exe_path]{simstrat_aed2_exe_path()}} to locate an already-installed executable,
\code{\link[=install_glm_aed]{install_glm_aed()}} for the equivalent GLM-AED installer this mirrors.
}
