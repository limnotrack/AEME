% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_dy_cd.R
\name{find_dy_cd_cfg}
\alias{find_dy_cd_cfg}
\title{Detect the DYRESM-CAEDYM configuration file within a directory}
\usage{
find_dy_cd_cfg(dir, must_exist = TRUE)
}
\arguments{
\item{dir}{character; the \code{dy_cd} model directory (not searched
recursively).}

\item{must_exist}{logical; abort if no \code{.stg} file is found. Default
\code{TRUE}.}
}
\value{
character; full path to the matching file, or \code{NA_character_} if
\code{must_exist = FALSE} and no \code{.stg} file was found.
}
\description{
A DYRESM-CAEDYM model directory holds a family of files that all share a
single \verb{<lakename>} prefix (\verb{<lakename>.cfg}, \code{.con}, \code{.stg}, \code{.inf},
\code{.wdr}, \code{.met}, \code{.pro}, \code{.int}). The \verb{<lakename>} is not knowable from
the directory name alone, so these helpers recover it from the \code{.stg}
file that is always present (the same approach \code{\link[=run_dy_cd]{run_dy_cd()}} itself
uses), and return the full path to the requested companion file.
}
\details{
\code{find_dy_cd_cfg()} returns the \code{.cfg} file (DYRESM-CAEDYM's top-level
configuration), analogous to \code{\link[=find_glm_nml]{find_glm_nml()}} for GLM-AED.
}
\examples{
\dontrun{
find_dy_cd_cfg(path_dy)
}

}
