% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_model_init.R
\name{set_glm_init}
\alias{set_glm_init}
\title{Set initial conditions for a GLM-AED simulation}
\usage{
set_glm_init(
  path_glm,
  temp = NULL,
  salt = NULL,
  wq_init = NULL,
  glm_file = find_glm_nml(path_glm)
)
}
\arguments{
\item{path_glm}{filepath; directory containing the GLM-AED configuration}

\item{temp}{numeric; new initial water temperature profile. Recycled to
the number of depths in \code{init_profiles}. \code{NULL} (default) leaves it
unchanged.}

\item{salt}{numeric; new initial salinity profile, same recycling rule as
\code{temp}. \code{NULL} (default) leaves it unchanged.}

\item{wq_init}{named list; new initial values for water quality variables,
e.g. \code{list(NIT_amm = 0.5, CHM_oxy = 300)}. Names must match
\code{init_profiles$wq_names} in the nml file. Each value is recycled across
depths. \code{NULL} (default) leaves water quality initial values unchanged.}

\item{glm_file}{filepath; path to the nml file to edit. Defaults to the
GLM hydrodynamic nml (\code{glm3.nml}/\code{glm4.nml}) found in \code{path_glm} via
\code{\link[=find_glm_nml]{find_glm_nml()}}.}
}
\value{
invisibly, the updated nml object
}
\description{
Thin wrapper for editing the initial temperature/salinity profile and
water-quality initial values of a GLM-AED hydrodynamic nml file in place,
without needing an \code{aeme} object. Intended for a GLM-AED-only workflow
where a user just wants to tweak initial conditions, run the model, and
load the output.
}
\details{
Existing profile depths (\code{init_profiles$the_depths}) are left unchanged --
\code{temp}/\code{salt}/\code{wq_init} values are recycled (via \code{\link[=rep_len]{rep_len()}}) across
however many depths are already defined.
}
\examples{
\dontrun{
set_glm_init(path_glm, temp = seq(20, 10, length.out = 10))
set_glm_init(path_glm, wq_init = list(NIT_amm = 0.5, CHM_oxy = 300))
}
}
