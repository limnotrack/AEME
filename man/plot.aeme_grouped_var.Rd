% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aeme_grouped_var.R
\name{plot.aeme_grouped_var}
\alias{plot.aeme_grouped_var}
\title{Plot a grouped variable as a line plot, coloured by its non-time
dimensions (e.g. one line per zone)}
\usage{
\method{plot}{aeme_grouped_var}(x, var_sim = NULL, ylim = NULL, ...)
}
\arguments{
\item{x}{an \code{aeme_grouped_var} object (see \code{\link[=new_grouped_var]{new_grouped_var()}}).}

\item{var_sim}{character; variable name, used for the plot title/y-axis
label. Default \code{NULL} (no title/label).}

\item{ylim}{numeric vector of length 2; y-axis limits. Default \code{NULL}
(ranged to the data).}

\item{...}{unused.}
}
\value{
A ggplot2 object (the long-format data frame instead, with a
warning, if \code{x} has no \code{"time"} dimension).
}
\description{
A thin wrapper around \code{\link[=as.data.frame.aeme_grouped_var]{as.data.frame.aeme_grouped_var()}} that plots the
resulting long-format data frame with ggplot2 -- one coloured line per
combination of the variable's dimensions other than \code{"time"} (e.g. one
line per sediment zone for a GLM-AED \verb{(nzones, time)} AED flux variable).
}
