% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_obs.R
\name{suggest_sim_period}
\alias{suggest_sim_period}
\title{Choose a simulation period from what the data can support}
\usage{
suggest_sim_period(
  aeme,
  vars_sim = NULL,
  spin_up = NULL,
  min_depths = 3L,
  use_profiles = TRUE,
  min_density = 0.25,
  min_years = 4L,
  align = c("none", "year"),
  year_start_month = 7L
)
}
\arguments{
\item{aeme}{Aeme object.}

\item{vars_sim}{Character. Variables the period must cover. Default
\code{NULL} uses every observed variable, which is rarely what you want -
name the variables you intend to fit.}

\item{spin_up}{Numeric. Days of spin-up required before \code{start}. Default
\code{NULL} takes the longest spin-up already set on the object.}

\item{min_depths}{Integer. Distinct depths a visit needs before it counts
as a profile. Default \code{3L}.}

\item{use_profiles}{Logical. Count only profiles when judging coverage.
Default \code{TRUE}; \code{FALSE} counts every sampling date. Variables of kind
\code{"scalar"} always count by date.}

\item{min_density}{Numeric. Drop leading and trailing years holding fewer
than this fraction of the median year's count. Default \code{0.25}; \code{0}
keeps the record whole.}

\item{min_years}{Integer. Fewest years a record must span before
\code{min_density} is applied. Default \code{4L}.}

\item{align}{Character. \code{"none"} (default) starts and stops on
observation dates; \code{"year"} snaps outward to whole years beginning
\code{year_start_month}.}

\item{year_start_month}{Integer. First month of the year used by
\code{align = "year"}. Default \code{7L}, the southern-hemisphere hydrological
year, so a period does not split a stratified season in half.}
}
\value{
An object of class \code{aeme_sim_period}: a list with \code{start},
\code{stop}, \code{spin_up}, \code{spin_up_start}, \code{limited_by} (what set each end),
\code{coverage} (per-variable counts inside the chosen period), \code{dropped}
(years trimmed at each end) and \code{summary} (the \code{\link[=summarise_obs]{summarise_obs()}}
result).
}
\description{
Picks \code{start} and \code{stop} dates for a simulation by intersecting three
things: where the forcing data exist, where the observations to be fitted
exist, and how much spin-up is needed before the first comparison.

The forcing constraint is the one most often missed. Observations
routinely predate the meteorological record by years, and a period chosen
from the observations alone can begin before the model has anything to
run on - or leave no room for spin-up, so the first observations are
compared against a model still relaxing from its initial condition.

Sparse leading and trailing years are trimmed by \code{min_density}, measured
in profiles rather than observations, because a depth-resolved
calibration is constrained by casts and not by surface grabs. Only the
ends are trimmed, so a lean year inside a dense record is kept and the
period stays contiguous.
}
\examples{
\dontrun{
aeme <- readRDS(system.file("extdata/aeme.rds", package = "AEME"))
p <- suggest_sim_period(aeme, vars_sim = c("HYD_temp", "CHM_oxy"),
                        spin_up = 365)
p
aeme <- set_sim_period(aeme, p)
}
}
\seealso{
\code{\link[=summarise_obs]{summarise_obs()}}, \code{\link[=set_sim_period]{set_sim_period()}}, \code{\link[=set_time]{set_time()}}
}
