% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_glm_nml.R
\name{find_glm_nml}
\alias{find_glm_nml}
\title{Detect the GLM hydrodynamic nml file within a directory}
\usage{
find_glm_nml(dir, must_exist = TRUE)
}
\arguments{
\item{dir}{character; the \code{glm_aed} model directory (not searched
recursively -- AED's own nml files live one level down in \verb{aed/}, so
they never collide with this pattern).}

\item{must_exist}{logical; abort if none is found. Default \code{TRUE}.}
}
\value{
character; full path to the matching file, or \code{NA_character_} if
\code{must_exist = FALSE} and none was found.
}
\description{
GLM's hydrodynamic namelist file has historically been named \code{glm3.nml},
but newer GLM releases may write e.g. \code{glm4.nml} instead. AEME treats any
file matching \verb{glm<version>.nml} in the top level of a \code{glm_aed} model
directory as \emph{the} hydrodynamic nml, so the rest of the package works the
same regardless of which GLM version produced it. If more than one such
file is present, the choice between them is resolved by
.preferred_glm_major_version() (see Details).
}
\details{
When multiple \verb{glm<version>.nml} files are found in the same directory
(e.g. a leftover \code{glm3.nml} alongside a newer \code{glm4.nml}), the one to use
is chosen in priority order:
\enumerate{
\item The GLM version pinned via the \code{AEME.glm_version} option (set by
\code{\link[=install_glm_aed]{install_glm_aed()}}, or by the caller directly).
\item Whichever GLM version is actually installed locally (see
\code{\link[=glm_exe_path]{glm_exe_path()}}), checked directly rather than trusting session state.
\item If neither can be determined, the highest version number among
the files present (e.g. \code{glm4.nml} over \code{glm3.nml}).
}
A message reports which file was picked and why, since an unused
\verb{glm<version>.nml} sitting in the directory is otherwise easy to miss.
}
\examples{
glm_dir <- file.path(tempdir(), "glm_aed")
dir.create(glm_dir, showWarnings = FALSE)
file.create(file.path(glm_dir, "glm3.nml"))
find_glm_nml(glm_dir)

}
