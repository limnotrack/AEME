% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_model_outputs.R
\name{read_model_outputs}
\alias{read_model_outputs}
\title{Read model outputs and format to AEME standard}
\usage{
read_model_outputs(
  nc = NULL,
  lake_dir,
  model,
  vars_sim = NULL,
  depths = NULL,
  dates = NULL,
  date_index = NULL,
  incl_fluxes = TRUE,
  output_hour = 0,
  phyto_pars = NULL,
  load_all = TRUE,
  use_dat = NULL
)
}
\arguments{
\item{nc}{Open netCDF object. If NULL, will open netCDF from lake_dir. This
is useful when reading multiple variables from the same file to avoid
reopening the file multiple times. Defaults to NULL.}

\item{lake_dir}{Directory of lake model outputs}

\item{model}{Model name. One of "gotm_wet", "glm_aed", or "dy_cd".}

\item{vars_sim}{Variables to extract in the AEME format e.g. "HYD_temp"}

\item{depths}{Depths to extract. If NULL, extract all model layer depths.
Defaults to NULL.}

\item{dates}{Dates to extract. If NULL, extract all dates. Defaults to NULL.}

\item{date_index}{Date index to extract. If NULL, extract all dates. Defaults
to NULL.}

\item{incl_fluxes}{Logical indicating whether to include flux variables.
Defaults to TRUE.}

\item{output_hour}{Hour of the day to extract (0-23). Defaults to 0.}

\item{phyto_pars}{Dataframe of phytoplankton parameters for GLM-AED model.
See \code{?read_glm_output} for details. Defaults to NULL.}

\item{load_all}{logical; for \code{model = "glm_aed"}, also load every other
variable present in the netCDF output beyond the declared \code{vars_sim} set
-- see \code{?read_glm_output}. Ignored for other models. Defaults to TRUE.}

\item{use_dat}{logical; for the Simstrat models only, read Simstrat's own
\verb{<var>_out.dat} text output via \code{\link{read_simstrat_dat}} instead of
the consolidated \code{output.nc}. Every other argument means the same thing
either way, so this only changes where the numbers are read from. \code{TRUE}
is the faster path -- it skips the netCDF entirely, and with
\code{load_all = FALSE} reads only the files the requested \code{vars_sim} need,
which is what a calibration wants. Defaults to \code{NULL}: read \code{output.nc}
when there is one, and fall back to the text output when there is not
(a run whose output was never converted, or converted with
\code{\link{write_simstrat_nc}}\code{(remove_dat = FALSE)} and the netCDF since
removed). Ignored when \code{nc} is supplied.}
}
\value{
List of model outputs in AEME standard format
}
\description{
Read model outputs and format to AEME standard
}
