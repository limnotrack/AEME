% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_model_init.R
\name{set_simstrat_init}
\alias{set_simstrat_init}
\title{Set initial conditions for a Simstrat simulation}
\usage{
set_simstrat_init(path_simstrat, temp = NULL, salt = NULL, wq_init = NULL)
}
\arguments{
\item{path_simstrat}{filepath; directory containing the Simstrat
configuration}

\item{temp}{numeric; new initial water temperature profile. Recycled to
the number of depths in \code{InitialConditions.dat}. \code{NULL} (default) leaves
it unchanged.}

\item{salt}{numeric; new initial salinity profile, same recycling rule as
\code{temp}. \code{NULL} (default) leaves it unchanged.}

\item{wq_init}{named list; new initial values for water quality
variables, keyed by \code{var_aeme} name, e.g. \code{list(NIT_amm = 0.5, CHM_oxy = 300)}. Each value is recycled across the same depths as
\code{InitialConditions.dat} and written to
\verb{<AED_initcond|AED2_initcond>/<var>_ini.dat}. \code{NULL} (default) leaves
water quality initial values unchanged. Requires a Simstrat-AED or
Simstrat-AED2 configuration (an \code{aed.nml}/\code{aed2.nml} in \code{path_simstrat}).}
}
\value{
invisibly, the updated initial conditions data.frame
}
\description{
Thin wrapper for editing the \code{InitialConditions.dat} file (temperature/
salinity) and, for a Simstrat-AED/AED2 simulation, the per-variable
\verb{<var>_ini.dat} override files of a Simstrat model directory in place,
without needing an \code{aeme} object. Intended for a Simstrat-only workflow
where a user just wants to tweak initial conditions, run the model, and
load the output.
}
\details{
The existing profile depths (and \code{U}, \code{V}, \code{k}, \code{eps} columns) in
\code{InitialConditions.dat} are left unchanged -- \code{temp}/\code{salt}/\code{wq_init}
values are recycled (via \code{\link[=rep_len]{rep_len()}}) across however many depths are
already defined.

Water quality initial conditions in Simstrat-AED/AED2 follow a two-layer
scheme: the \code{aed.nml}/\code{aed2.nml} \verb{<var>_initial} field is a fallback
constant used for every grid cell, and an optional per-variable
\verb{<path_aed_initial>/<var>_ini.dat} (depth, value) profile -- if present --
overrides it, interpolated onto the vertical grid. \code{wq_init} writes this
override file directly (it takes precedence over the nml default
regardless of the nml value), rather than editing the nml fallback.
Which coupler is in use (Simstrat-AED vs Simstrat-AED2, and therefore
the \verb{AED_initcond/}/\verb{AED2_initcond/} directory and variable-naming
convention) is detected from whichever of \code{aed.nml}/\code{aed2.nml} exists in
\code{path_simstrat}.
}
\examples{
\dontrun{
set_simstrat_init(path_simstrat, temp = seq(20, 10, length.out = 10))
set_simstrat_init(path_simstrat, wq_init = list(NIT_amm = 0.5, CHM_oxy = 300))
}
}
