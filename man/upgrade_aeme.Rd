% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{upgrade_aeme}
\alias{upgrade_aeme}
\title{Upgrade an Aeme object to the current AEME version}
\usage{
upgrade_aeme(aeme, quiet = FALSE)
}
\arguments{
\item{aeme}{An \code{Aeme} object.}

\item{quiet}{Logical; suppress the summary of applied changes. Default
\code{FALSE}.}
}
\value{
The \code{Aeme} object, migrated to the current layout, with
\code{configuration$aeme_upgraded} set to the installed AEME version.
}
\description{
Older \code{Aeme} objects -- loaded from \code{.rds} files written by a previous
version of AEME -- can be missing list elements, use since-renamed slot
names, or carry data frames with an older column layout. Most of the
package tolerates this, but some code paths (and \code{\link[=build_aeme]{build_aeme()}} in
particular) assume the current layout.

\code{upgrade_aeme()} applies every structural migration AEME knows about, in
order, each one idempotent so the function is safe to run repeatedly. It
does \strong{not} rebuild model configuration
(\verb{configuration$<model>$hydrodynamic}) or model output -- those only come
from \code{\link[=build_aeme]{build_aeme()}} / \code{\link[=run_aeme]{run_aeme()}}. Run \code{upgrade_aeme()} first, then
rebuild if you need the model files refreshed.

Migrations applied (see also migrate_aeme(), the silent worker):
\itemize{
\item \code{time$spin_up}, \code{inflows$factor}, \code{outflows$factor},
\code{configuration}: backfill entries for models added to AEME after the
object was created (e.g. \code{simstrat_aed2}, \code{simstrat_aed}).
\item \code{outflows}: rename the legacy \code{lvl} / \code{outflow_lvl} element to
\code{elevation}.
\item \code{output}: add a \code{NULL} placeholder per model and coerce
\code{n_members} to integer.
\item \code{observations$level}: coerce a legacy tibble to a plain data frame
and ensure a \code{var_aeme} column.
\item \code{configuration}: backfill scalar build defaults (\code{ext_elev},
\code{calc_wbal}, \code{wb_method}, \code{calc_wlev}, \code{hum_type}, \code{est_swr_hr},
\code{use_bgc}) from \code{config_defaults()}.
\item \code{parameters}: reorder columns to \code{\link[=param_colnames]{param_colnames()}} order.
}
}
\seealso{
\code{\link[=build_aeme]{build_aeme()}}, \code{\link[=check_aeme]{check_aeme()}}
}
