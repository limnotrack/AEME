% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag_aeme.R
\name{diag_nutrient_budget}
\alias{diag_nutrient_budget}
\title{Per-variable BGC bias vs observations}
\usage{
diag_nutrient_budget(
  aeme,
  model,
  candidates = c("CHM_oxy", "NIT_amm", "NIT_nit", "NIT_tn", "PHS_frp", "PHS_tp",
    "PHY_tchla"),
  min_obs = 8,
  bias_ok_frac = 0.25
)
}
\arguments{
\item{aeme}{Aeme object; already built and run for \code{model}.}

\item{model}{character; single model code to diagnose (see
\code{\link{check_model}}).}

\item{candidates}{character vector; AEME variable names to check.}

\item{min_obs}{integer; below this many in-window observations, the
variable is skipped.}

\item{bias_ok_frac}{numeric; a variable is classified \code{"ok"} when the
absolute mean bias is below this fraction of the observed mean (a
variable-appropriate relative threshold, since oxygen/nitrate/
chlorophyll live on very different absolute scales).}
}
\value{
data frame with columns \code{var}, \code{mean_bias}, \code{mean_obs},
\code{rel_bias}, \code{n}, \code{direction} (\code{"model too high"}, \code{"model too low"} or
\code{"ok"}). Zero rows if no candidate variable has enough usable data.
}
\description{
Reports bias for whichever candidate variables this lake actually has
usable data for.
}
