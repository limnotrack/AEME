% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_simstrat_inflows.R
\name{set_simstrat_inflows}
\alias{set_simstrat_inflows}
\title{Set inflow data for a Simstrat-AED2 simulation directory}
\usage{
set_simstrat_inflows(
  path_simstrat,
  inf,
  inf_factor = 1,
  use_bgc = NULL,
  model_controls = NULL,
  ref_year = NULL,
  par_file = file.path(path_simstrat, "simstrat.par")
)
}
\arguments{
\item{path_simstrat}{filepath; to Simstrat-AED2 directory}

\item{inf}{named list of data.frames, one per inflow. Each must have a
\code{Date} column plus \code{HYD_flow}, \code{HYD_temp}, \code{CHM_salt} columns -- see
\code{\link[=add_inflow]{add_inflow()}} for the expected schema. Multiple inflows are merged into
a single series (Simstrat only accepts one): flow is summed and the
scalars (temperature, salinity, BGC) are flow-weighted so total load is
conserved -- see \code{make_inf_simstrat()}.}

\item{inf_factor}{numeric; scaling factor applied to all inflow flow
rates. Default is \code{1}.}

\item{use_bgc}{logical; also write AED2 inflow concentration files.
Defaults to the existing \code{ModelConfig.CoupleAED2} setting in
\code{simstrat.par}.}

\item{model_controls}{data.frame of loaded model controls (see
\code{\link[=get_model_controls]{get_model_controls()}}), required when \code{use_bgc = TRUE}.}

\item{ref_year}{integer; Simstrat's \verb{Simulation.Reference year}. Defaults
to the value already in \code{simstrat.par}.}

\item{par_file}{filepath; path to the \code{simstrat.par} file to read
\code{use_bgc}/\code{ref_year} defaults from. Defaults to \code{simstrat.par} in
\code{path_simstrat}.}
}
\value{
invisibly, \code{NULL}
}
\description{
Thin, \code{aeme}-free wrapper around the internal inflow writer used by
\code{\link[=build_aeme]{build_aeme()}}. Writes \code{Qinp.dat}/\code{Tinp.dat}/\code{Sinp.dat} (and, if BGC is
coupled, \verb{AED2_inflow/*.dat}) into \code{path_simstrat}.
}
\examples{
\dontrun{
set_simstrat_inflows(path_simstrat, inf = list(stream1 = inflow_df))
}
}
