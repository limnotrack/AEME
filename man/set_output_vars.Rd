% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_output_vars.R
\name{set_output_vars}
\alias{set_output_vars}
\title{Restrict a model's written output to the variables of interest}
\usage{
set_output_vars(aeme, model, vars, mass_balance = TRUE, ens_n = 1)
}
\arguments{
\item{aeme}{An \code{Aeme} object carrying a configuration for \code{model}.}

\item{model}{Character. A single model, one of \code{"glm_aed"},
\code{"gotm_wet"}, \code{"simstrat_aed"}, \code{"simstrat_aed2"}, \code{"dy_cd"}.}

\item{vars}{Character. AEME variable names to keep, e.g.
\code{c("HYD_temp", "CHM_oxy")}. Mapped to each model's own output names via
\code{\link{key_naming}}; names with no mapping for \code{model} are dropped
with a warning.}

\item{mass_balance}{Logical. For \code{"glm_aed"} only: keep the GLMv4
\verb{&mass_balance} diagnostic CSV? Default \code{TRUE}. Ignored for other
models.}

\item{ens_n}{Integer. Ensemble member whose configuration slot is
updated. Default \code{1}.}
}
\value{
\code{aeme}, with its \code{configuration()} updated.
}
\description{
By default every AEME model writes its full state at every output step.
When only a few variables are needed - as in calibration or sensitivity
analysis, where the objective is computed from one or two variables - the
rest is wasted disk I/O (and, for Simstrat and GOTM, wasted files).
\code{set_output_vars()} rewrites the output section of a model's configuration
so that only \code{vars}, plus the handful of internals AEME always needs to
read a result back (water level, the depth grid, temperature), are
written.
}
\details{
The change is made on the in-memory configuration; call
\code{\link{write_configuration}} (or \code{\link{build_aeme}}) to write
it to disk. \code{\link{build_aeme}}'s \code{output_vars} argument applies this
automatically at build time.
\itemize{
\item \strong{GLM} always writes the full netCDF - its gridded variables cannot be
sub-selected - so this only drops the \verb{&output} \verb{csv_point_*} keys
(disabling the fixed-depth \code{WQ_*.csv} files) and, when
\code{mass_balance = FALSE}, the whole \verb{&mass_balance} block (disabling
\code{mass_balance.csv}). The whole-lake \code{lake.csv} (\code{csv_lake_fname}) is
left on: GLM 4.x only writes the netCDF diagnostic scalars
(\code{lake_level}, ...) while that CSV is open, and AEME needs \code{lake_level}
to read a GLM result back.
\item \strong{Simstrat} writes one \verb{*_out.dat} per variable; this sets
\code{Output$All = FALSE} and lists only the needed variables.
\item \strong{GOTM} replaces the \verb{/*} (all-variables) output source with an
explicit list.
\item \strong{DYRESM} has a fixed output form and is returned unchanged.
}
}
\examples{
aeme <- readRDS(system.file("extdata/aeme.rds", package = "AEME"))
path <- tempdir()
aeme <- build_aeme(path = path, aeme = aeme, model = "glm_aed",
                   model_controls = get_model_controls(), ext_elev = 5)
aeme <- set_output_vars(aeme, "glm_aed", "HYD_temp", mass_balance = FALSE)
write_configuration(aeme, model = "glm_aed", path = path)
}
\seealso{
\code{\link{set_vars_sim}}, \code{\link{write_configuration}},
\code{\link{get_output_vars}}
}
