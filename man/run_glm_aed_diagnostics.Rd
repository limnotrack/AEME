% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_glm_aed_diagnostics.R
\name{run_glm_aed_diagnostics}
\alias{run_glm_aed_diagnostics}
\title{Run GLM-AED diagnostics}
\usage{
run_glm_aed_diagnostics(
  aeme,
  model,
  groups = NULL,
  depth_collapse = "mean",
  plot = TRUE,
  use_bounds = TRUE,
  print_table = TRUE
)
}
\arguments{
\item{model}{Model name. One of "gotm_wet", "glm_aed", or "dy_cd".}

\item{groups}{character vector selecting catalogue entries.
Accepts:
  - catalogue entry names (e.g. "nitrogen_state"),
  - element codes ("O","N","P","Phy"),
  - types ("state","process").
Default NULL = all entries.}

\item{depth_collapse}{"mean", "surface" or "max" — reduce 3D variables}

\item{plot}{draw combined plots, grouped by element}

\item{use_bounds}{add dashed lines to plots showing expected bounds 
(from catalogue)}

\item{print_table}{print the kable summary}
}
\value{
invisibly, list(summary, plots, data)
}
\description{
Run GLM-AED diagnostics
}
\examples{
aeme_dir <- system.file("extdata/lake/", package = "AEME")
# Copy files from package into tempdir
path <- tempdir()
aeme <- yaml_to_aeme(path = aeme_dir, "aeme.yaml")
vars_sim <- c("HYD_strat", "HYD_temp", "HYD_thmcln", "HYD_schstb", 
              "CHM_oxycln", "CHM_oxynal",
              "NIT_tn", "PHS_tp", "PHY_tchla")
model_controls <- get_model_controls(use_bgc = TRUE)
model_controls <- set_vars_sim(model_controls = model_controls,
                               vars_sim = vars_sim)
model <- c("glm_aed")
aeme <- build_aeme(path = path, aeme = aeme, model = model,
                   model_controls = model_controls,
                   ext_elev = 5, use_bgc = TRUE)

aeme <- run_aeme(aeme)
out <- run_glm_aed_diagnostics(aeme = aeme)
}
