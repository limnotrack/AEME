% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_output_base.R
\name{plot_output_base}
\alias{plot_output_base}
\title{Base plotting function for AEME output}
\usage{
plot_output_base(
  aeme,
  model,
  var_sim = "HYD_temp",
  ens_n = 1,
  var_lims = NULL,
  ylim = NULL,
  plot_width = 400,
  plot_height = 200,
  bar_width = 0.08
)
}
\arguments{
\item{aeme}{Aeme object.}

\item{model}{character vector; model(s) to plot. If missing, all models
in the Aeme object will be plotted.}

\item{var_sim}{string; of variable to plot}

\item{ens_n}{integer; ensemble number to plot. Default is 1.}

\item{var_lims}{numeric vector of length 2; limits for the variable.
Defaults to NULL and will generate common limits for all variables.}

\item{ylim}{numeric vector of length 2; limits for the y-axis. Defaults to
NULL and calculates this based on the data to be plotted.}
}
\value{
A list of matrices with the plotted output, invisibly.
}
\description{
Base plotting function for AEME output
}
\examples{
\dontrun{
tmpdir <- tempdir()
aeme_dir <- system.file("extdata/lake/", package = "AEME")
# Copy files from package into tempdir
file.copy(aeme_dir, tmpdir, recursive = TRUE)
path <- file.path(tmpdir, "lake")
aeme <- yaml_to_aeme(path = path, "aeme.yaml")
path <- tempdir()
aeme <- build_aeme(aeme = aeme, model = model, path = path, 
                   ext_elev = 5) |>
  run_aeme(aeme)

plot_output_base(aeme)

# Can also use plot_output() with `backend` set to "base"
plot_output(aeme, backend = "base")
}
}
