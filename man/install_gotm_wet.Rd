% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_gotm_wet.R
\name{install_gotm_wet}
\alias{install_gotm_wet}
\title{Install a GOTM-WET executable for AEME}
\usage{
install_gotm_wet(
  version = "latest",
  os = NULL,
  repo = "limnotrack/AEME",
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{version}{Character. The GOTM-WET version to install, e.g.
\code{"2023.2.0"}. Use \code{\link[=list_gotm_wet_versions]{list_gotm_wet_versions()}} to see what's available.
Defaults to \code{"latest"}, which resolves to the highest version number
available for the current platform.}

\item{os}{Character. One of \code{"windows"}, \code{"macos"}, or \code{"linux"}.
Defaults to the platform R is currently running on; you shouldn't
normally need to set this. Note that only a Windows build currently
exists (no source is compiled for other platforms), so \code{macos}/\code{linux}
will report no versions available.}

\item{repo}{Character. The \code{"owner/repo"} GitHub repository that GOTM-WET
binaries are attached to. Defaults to \code{"limnotrack/AEME"}.}

\item{force}{Logical. If \code{FALSE} (the default) and this version is
already installed for this platform, the download/verification steps
are skipped and the existing path is returned. Set to \code{TRUE} to
re-download and reinstall anyway.}

\item{quiet}{Logical. If \code{TRUE}, suppresses progress messages (download
errors and checksum failures still raise, and are never silenced).}
}
\value{
Invisibly, the file path to the installed GOTM-WET executable.
}
\description{
Downloads a pre-compiled GOTM (coupled with the WET biogeochemical
library) binary for the current platform, verifies it against its
published SHA256 checksum, and installs it into a persistent user cache
directory. Binaries are attached as assets to AEME's GitHub releases, with
the GOTM-WET version encoded in the asset filename (e.g.
\verb{gotm-windows-2023.2.0.zip}), mirroring \code{\link[=install_glm_aed]{install_glm_aed()}}. Because a
given GOTM-WET version isn't tied to any one AEME release, this function
searches across all releases of \code{repo} to find the release that has the
requested version's asset attached.
}
\details{
Binaries are cached under \code{\link[tools:R_user_dir]{tools::R_user_dir("AEME", "data")}},
in an \verb{<os>/<version>/} subdirectory (the same root \code{\link[=install_glm_aed]{install_glm_aed()}}
and \code{\link[=install_simstrat_aed2]{install_simstrat_aed2()}} use -- these coexist there because the
installed files themselves are named differently, \code{gotm}/\code{gotm.exe} vs
\code{glm}/\code{glm.exe} vs \code{simstrat}/\code{simstrat.exe}).

Every binary is published alongside a \code{.sha256} checksum file in the same
release. This function downloads both, recomputes the SHA256 of the
downloaded zip locally, and compares it to the published value before
extracting anything. If the checksums don't match, or if no checksum file
is found at all, installation is aborted and nothing is extracted -- this
function will not install an unverified binary under any circumstances.
}
\examples{
\dontrun{
install_gotm_wet(version = "2023.2.0")

# Force re-download and reinstall
install_gotm_wet(version = "2023.2.0", force = TRUE)
}

}
\seealso{
\code{\link[=list_gotm_wet_versions]{list_gotm_wet_versions()}} to discover available versions,
\code{\link[=gotm_wet_exe_path]{gotm_wet_exe_path()}} to locate an already-installed executable,
\code{\link[=install_glm_aed]{install_glm_aed()}} for the equivalent GLM-AED installer this mirrors.
}
