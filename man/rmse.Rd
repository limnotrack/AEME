% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{rmse}
\alias{rmse}
\title{Root Mean Square Error (RMSE) fit function}
\usage{
rmse(obs, sim, na.rm = TRUE)
}
\arguments{
\item{obs}{Numeric vector of observed values.}

\item{sim}{Numeric vector of simulated (modelled) values.}

\item{na.rm}{Logical; should missing values (NA) in either vector be removed
pairwise before computation? Default is \code{TRUE}.}
}
\value{
numeric; root mean square error, in the same units as \code{obs}/\code{sim}.
}
\description{
Calculates the root mean square error between observed and modelled values:
}
\details{
\deqn{\text{RMSE} = \sqrt{\frac{1}{n} \sum_{i=1}^n (\text{sim}_i - \text{obs}_i)^2}}

Already \code{0} (perfect fit) at its best and increasing as fit
worsens, so it is minimise-oriented as-is and needs no \verb{_loss} companion
\itemize{
\item pass it straight into \code{FUN_list}, which \code{\link{calib_aeme}} and
\code{\link{run_and_fit}} minimise.
}

Like \code{\link{mae}}, RMSE stays in the variable's native units and so
is not directly comparable across variables with different units or
magnitudes when combined in a multi-variable \code{FUN_list} - see
\code{\link{mae}} for details. RMSE additionally squares errors before
averaging, so - like NSE - it weights large deviations more heavily than
MAE does.
}
\examples{
obs <- c(1, 2, 3, 4)
sim <- c(1.1, 2.1, 2.9, 4.2)
rmse(obs, sim)

}
