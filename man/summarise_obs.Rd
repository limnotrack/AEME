% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_obs.R
\name{summarise_obs}
\alias{summarise_obs}
\title{Summarise what the observations actually contain}
\usage{
summarise_obs(aeme, vars_sim = NULL, min_depths = 3L)
}
\arguments{
\item{aeme}{Aeme object.}

\item{vars_sim}{Character. Variables (\code{var_aeme} values) to summarise.
Default \code{NULL} summarises every observed variable.}

\item{min_depths}{Integer. Distinct depths a visit needs before it counts
as a profile. Default \code{3L}.}
}
\value{
An object of class \code{aeme_obs_summary}: a list with
\describe{
\item{\code{variables}}{one row per variable - \code{n_obs}, \code{n_dates},
\code{first}, \code{last}, \code{n_years}, \code{n_months} (distinct calendar months,
i.e. seasonal coverage), \code{n_profiles}, \code{profile_frac},
\code{median_depths}, \code{depth_min}, \code{depth_max} and \code{kind}.}
\item{\code{years}}{one row per variable per calendar year - \code{n_obs},
\code{n_dates}, \code{n_profiles}.}
\item{\code{forcing}}{one row per forcing series - \code{source}, \code{first},
\code{last}, \code{n}.}
\item{\code{window}}{the date range over which every forcing series has
data, i.e. the widest period the model can be run over.}
}
}
\description{
Reports, for every observed variable: how many observations there are,
when they span, how they are distributed across years and seasons, and
whether they are depth \emph{profiles} or single-depth samples.

The profile question is the one that usually decides whether a
calibration is possible. A variable sampled hundreds of times can still be
useless for constraining stratification if every one of those samples is a
surface grab, and observation counts alone hide that completely.

Each variable is classified as one of
\describe{
\item{\code{"profile"}}{at least half its sampling dates carry \code{min_depths}
or more distinct depths - a depth-resolved record.}
\item{\code{"discrete"}}{depths are recorded, but most visits carry fewer
than \code{min_depths} of them - surface grabs, or surface/bottom pairs.}
\item{\code{"scalar"}}{no depth information at all, e.g. water level.}
}

The summary also carries the date range of every \emph{forcing} series - met,
inflows, outflows - because those, not the observations, are usually what
limits how early a simulation can start. See \code{\link[=suggest_sim_period]{suggest_sim_period()}},
which turns this summary into a runnable period.
}
\examples{
\dontrun{
aeme <- readRDS(system.file("extdata/aeme.rds", package = "AEME"))
s <- summarise_obs(aeme)
s
s$variables
s$years[s$years$var_aeme == "HYD_temp", ]
}
}
\seealso{
\code{\link[=suggest_sim_period]{suggest_sim_period()}}, \code{\link[=set_sim_period]{set_sim_period()}}, \code{\link[=get_obs]{get_obs()}}
}
