% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_glm_aed.R
\name{list_glm_versions}
\alias{list_glm_versions}
\title{List available GLM versions}
\usage{
list_glm_versions(repo = "limnotrack/AEME", os = NULL)
}
\arguments{
\item{repo}{Character. The \code{"owner/repo"} GitHub repository to search.
Defaults to \code{"limnotrack/AEME"}.}

\item{os}{Character or \code{NULL}. If supplied, one of \code{"windows"},
\code{"macos"}, or \code{"linux"}, restricting results to that platform. If
\code{NULL} (the default), all platforms are included.}
}
\value{
A data frame with one row per available (release, platform,
version) combination, with columns \code{package_release}, \code{os}, and
\code{glm_version}. Returns a zero-row data frame (with a message) if
nothing is found.
}
\description{
Scans all releases of \code{repo} and returns every GLM version that has a
binary attached, along with which AEME release (package version) and
platform each one belongs to.
}
\examples{
\dontrun{
list_glm_versions()
list_glm_versions(os = "windows")
}

}
