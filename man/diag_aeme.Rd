% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag_aeme.R
\name{diag_aeme}
\alias{diag_aeme}
\title{Diagnose the three baseline budgets (water, heat, nutrient) at once}
\usage{
diag_aeme(aeme, model, use_bgc, ...)
}
\arguments{
\item{aeme}{Aeme object; already built and run for \code{model}.}

\item{model}{character; single model code to diagnose (see
\code{\link{check_model}}).}

\item{use_bgc}{logical; also run \code{\link{diag_nutrient_budget}}. Set
to \code{FALSE} when \code{aeme} was run without the BGC module active. Automatically
derived from the \code{use_bgc} flag in the \code{aeme} object when omitted.}
}
\value{
an \code{aeme_diag} object: list with \code{aeme}, \code{model},
\code{water_balance}, \code{heat_budget} and \code{nutrient_budget} (\code{NULL} when
\code{use_bgc = FALSE}).
}
\description{
Runs \code{\link{diag_water_balance}}, \code{\link{diag_heat_budget}} and,
when \code{use_bgc = TRUE}, \code{\link{diag_nutrient_budget}} against an
already built and run \code{\link{Aeme-class}} object, and reports a
one-line classification for each. Meant as the entry point for a baseline
(shipped-default) simulation, before committing to a staged calibration
design: which parameters go in which stage, and what bounds, depends on
which of these budgets is actually biased.
}
\details{
\code{aeme} must already have current output for \code{model} (i.e. built with
\code{\link{build_aeme}} and run with \code{\link{run_aeme}}) - this
function does not build or run anything itself.
}
