% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_glm_aed.R
\name{install_glm_aed}
\alias{install_glm_aed}
\title{Install a GLM executable for AEME}
\usage{
install_glm_aed(
  version = "latest",
  os = NULL,
  repo = "limnotrack/AEME",
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{version}{Character. The GLM version to install, e.g. \code{"3.9.108"}.
Use \code{\link[=list_glm_versions]{list_glm_versions()}} to see what's available. Defaults to \code{"latest"},
which resolves to the highest version number available for the current
platform.}

\item{os}{Character. One of \code{"windows"}, \code{"macos"}, or \code{"linux"}.
Defaults to the platform R is currently running on; you shouldn't
normally need to set this.}

\item{repo}{Character. The \code{"owner/repo"} GitHub repository that GLM
binaries are attached to. Defaults to \code{"limnotrack/AEME"}.}

\item{force}{Logical. If \code{FALSE} (the default) and this version is
already installed for this platform, the download/verification steps
are skipped and the existing path is returned. Set to \code{TRUE} to
re-download and reinstall anyway.}

\item{quiet}{Logical. If \code{TRUE}, suppresses progress messages (download
errors and checksum failures still raise, and are never silenced).}
}
\value{
Invisibly, the file path to the installed GLM executable.
}
\description{
Downloads a pre-compiled GLM binary for the current platform, verifies it
against its published SHA256 checksum, and installs it into a persistent
user cache directory. Binaries are attached as assets to AEME's GitHub
releases (tagged with the AEME package version, e.g. \code{"0.4.0"}), with the
GLM version encoded in the asset filename (e.g. \verb{glm-windows-3.9.108.zip}).
Because a given GLM version isn't tied to any one AEME release, this
function searches across all releases of \code{repo} to find the release that
has the requested version's asset attached.
}
\details{
Binaries are cached under \code{\link[tools:R_user_dir]{tools::R_user_dir("AEME", "data")}},
in a \verb{<os>/<version>/} subdirectory - by default this resolves to
\verb{~/.local/share/R/AEME} on Linux, \verb{~/Library/Application Support/org.R-project.R/R/AEME}
on macOS, and \verb{\%APPDATA\%\\R\\data\\R\\AEME} on Windows (overridable via the
\code{R_USER_DATA_DIR}/\code{XDG_DATA_HOME} environment variables - see
\link[tools:R_user_dir]{tools::R_user_dir} for details). Multiple GLM versions can be installed
side by side and switched between via the \code{version} argument to
\code{\link[=glm_exe_path]{glm_exe_path()}} without re-downloading.

Every binary is published alongside a \code{.sha256} checksum file in the same
release. This function downloads both, recomputes the SHA256 of the
downloaded zip locally, and compares it to the published value before
extracting anything. If the checksums don't match, or if no checksum file
is found at all, installation is aborted and nothing is extracted - this
function will not install an unverified binary under any circumstances.
}
\examples{
\dontrun{
install_glm_aed(version = "3.9.108")

# Force re-download and reinstall
install_glm_aed(version = "3.9.108", force = TRUE)
}

}
\seealso{
\code{\link[=list_glm_versions]{list_glm_versions()}} to discover available versions,
\code{\link[=glm_exe_path]{glm_exe_path()}} to locate an already-installed executable.
}
