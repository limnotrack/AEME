% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_models.R
\name{install_models}
\alias{install_models}
\title{Install the latest available binary for every AEME model}
\usage{
install_models(
  model = NULL,
  os = NULL,
  repo = "limnotrack/AEME",
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{model}{Character vector of model names to install, in either
display (\code{"GLM-AED"}) or code (\code{"glm_aed"}) form. Defaults to every
model AEME knows about (see \code{\link[=list_models]{list_models()}}).}

\item{os}{Character. One of \code{"windows"}, \code{"macos"}, or \code{"linux"}.
Defaults to the platform R is currently running on.}

\item{repo}{Character. The \code{"owner/repo"} GitHub repository release
assets are attached to. Defaults to \code{"limnotrack/AEME"}.}

\item{force}{Logical. If \code{FALSE} (the default), a model already
installed at the resolved "latest" version is left alone. Set to
\code{TRUE} to re-download and reinstall every model regardless.}

\item{quiet}{Logical. If \code{TRUE}, suppresses the per-model progress
messages from each installer (the final summary is still printed
unless silenced separately - see Value).}
}
\value{
Invisibly, a named character vector with one entry per requested
model: the installed executable path on success, or \code{NA} for any model
that was skipped or failed.
}
\description{
Convenience wrapper around \code{\link[=install_glm_aed]{install_glm_aed()}}, \code{\link[=install_gotm_wet]{install_gotm_wet()}},
\code{\link[=install_dy_cd]{install_dy_cd()}}, and \code{\link[=install_simstrat_aed2]{install_simstrat_aed2()}} that installs the latest
version of each, for whichever models actually have a binary published
for the current platform. A model with no release asset for this OS (or
no release assets published at all yet, e.g. GOTM-WET/DYRESM-CAEDYM
before their first upload) is reported and skipped rather than aborting
the whole call - one missing model shouldn't block installing the
others.
}
\examples{
\dontrun{
install_models()
install_models(model = c("glm_aed", "simstrat_aed2"))
install_models(force = TRUE)
}

}
\seealso{
\code{\link[=install_glm_aed]{install_glm_aed()}}, \code{\link[=install_gotm_wet]{install_gotm_wet()}}, \code{\link[=install_dy_cd]{install_dy_cd()}},
\code{\link[=install_simstrat_aed2]{install_simstrat_aed2()}} to install a single model with more control
(specific version, etc.).
}
