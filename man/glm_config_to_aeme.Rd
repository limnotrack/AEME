% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glm_config_to_aeme.R
\name{glm_config_to_aeme}
\alias{glm_config_to_aeme}
\title{Reconstruct an Aeme object from a GLM-AED model configuration}
\usage{
glm_config_to_aeme(
  nml_file,
  model_controls = NULL,
  spin_up = 2,
  read_params = FALSE
)
}
\arguments{
\item{nml_file}{character; path to a GLM hydrodynamic nml file (e.g.
\code{glm3.nml}, \code{glm4.nml}), typically inside a \verb{<id>_<name>/glm_aed/}
directory written by \code{\link[=build_aeme]{build_aeme()}}.}

\item{model_controls}{data.frame; model configuration, typically loaded
via \code{\link[=get_model_controls]{get_model_controls()}}. If \code{NULL} (default), one is generated with
\code{\link[=get_model_controls]{get_model_controls()}}, using biogeochemistry state (see Details) to set
\code{use_bgc}.}

\item{spin_up}{numeric; number of spin-up days assumed to have been
subtracted from \code{time$start} when the nml file was written (see
Details). Default \code{2}, matching \code{\link[=aeme_constructor]{aeme_constructor()}}'s own default.}

\item{read_params}{logical; also recover a \code{parameters} data frame (as set
by \code{\link[=add_param]{add_param()}}) by cross-referencing every parameter known to
\code{\link[=get_aeme_parameters]{get_aeme_parameters()}} against the value actually present in the GLM
nml, \code{aed/aed.nml}, and the AED parameter CSVs. Default \code{FALSE}.}
}
\value{
An \code{Aeme} object.
}
\description{
The inverse of \code{\link[=build_aeme]{build_aeme()}} for the GLM-AED model. Parses an existing
GLM hydrodynamic nml file (\code{glm3.nml}, \code{glm4.nml}, or any future
\verb{glm<version>.nml}; see \code{find_glm_nml()}) and the sibling files it
references (meteorology, inflows, outflows, AED biogeochemistry) and
reassembles them into an \code{Aeme} object.
}
\details{
Several pieces of the original \code{Aeme} object cannot be recovered exactly
from the GLM-AED files alone, and are approximated:
\itemize{
\item \code{lake$id} is taken from the \verb{<id>_<name>} lake directory naming
convention used by \code{\link[=build_aeme]{build_aeme()}} (the parent of \code{nml_file}'s
directory). If the directory does not follow that convention,
\code{"0001"} is used and a warning is issued.
\item \code{lake$name} is whatever is stored in \code{morphometry$lake_name},
which \code{\link[=build_aeme]{build_aeme()}} always writes in lower case -- the original
capitalisation is not recoverable.
\item \code{lake$elevation} (and the hypsograph's elevation datum) is taken
as \code{crest_elev}, i.e. the top of the hypsograph as written to the nml.
If the hypsograph was extended with \code{ext_elev} at build time, this will
not match the original \code{lake$elevation}.
\item \code{time$start} has \code{spin_up} days added back on, since
\code{\link[=build_aeme]{build_aeme()}} subtracts each model's spin-up period from \code{time$start}
before writing it to the nml file. The true original spin-up is not
stored anywhere in the GLM-AED files, so \code{spin_up} is a caller-supplied
guess (default: \code{aeme_constructor()}'s own default of 2 days).
\item \code{inflows()$factor} / \code{outflows()$factor} are assumed to be \code{1} --
any factor applied at build time is already baked into the written
\code{.csv} values and cannot be separated back out.
\item \code{parameters} (when \code{read_params = TRUE}) only recovers scalar,
numeric parameters known to AEME's parameter catalogue
(\code{\link[=get_aeme_parameters]{get_aeme_parameters()}}); \code{min}/\code{max}/\code{group} come from that
catalogue, not from the lake-specific files (which do not store them).
Logical/character nml values (e.g. \code{glm_setup::non_avg}) and
vector-valued/indexed nml parameters (e.g. per sediment zone) are
skipped, since the catalogue has no lake-specific notion of a vector's
true length and recovering only part of it would corrupt the field if
written back via \code{\link[=input_model_parameters]{input_model_parameters()}}.
}

The returned object has \code{configuration()$calc_wbal}, \code{calc_wlev}, and
\code{ext_elev} set to \code{FALSE}/\code{FALSE}/\code{0}, rather than
\code{\link[=build_aeme]{build_aeme()}}'s own defaults of \code{TRUE}/\code{TRUE}/\code{0}. The loaded
\code{inflows()}/\code{outflows()}/hypsograph already reflect a finished water
balance and lake-level calculation; leaving \code{calc_wbal}/\code{calc_wlev} at
their \code{build_aeme()} defaults would make a subsequent
\code{build_aeme(aeme = ., use_aeme = TRUE)} call recompute and silently
overwrite those loaded values (\code{use_aeme = TRUE} only preserves the raw
nml text -- it does not by itself disable the water-balance/lake-level
recalculation that runs before the per-model config files are written).
Passing \code{calc_wbal}/\code{calc_wlev}/\code{ext_elev} explicitly to a later
\code{\link[=build_aeme]{build_aeme()}} call overrides these stored values as usual.

The GLM version this configuration was read from (e.g. \code{"glm3.nml"} or
\code{"glm4.nml"}) is recorded in \code{configuration()$glm_aed$hydrodynamic_file},
so a later \code{\link[=write_configuration]{write_configuration()}} or \code{\link[=build_aeme]{build_aeme()}} call writes it back
out under the same filename rather than assuming \code{glm3.nml}.
}
\examples{
aeme_dir <- system.file("extdata/lake/", package = "AEME")
path <- file.path(tempdir(), "glm_config_to_aeme_example")
aeme <- yaml_to_aeme(path = aeme_dir, "aeme.yaml")
model_controls <- get_model_controls()
aeme <- aeme |>
  build_aeme(path = path, model = "glm_aed", model_controls = model_controls,
             ext_elev = 5)
nml_file <- file.path(get_lake_dir(aeme, path), "glm_aed", "glm3.nml")
aeme2 <- glm_config_to_aeme(nml_file)
}
