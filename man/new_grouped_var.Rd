% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aeme_grouped_var.R
\name{new_grouped_var}
\alias{new_grouped_var}
\title{Construct a "grouped" (non depth x time) model output variable}
\usage{
new_grouped_var(value, dim_names, dim_values)
}
\arguments{
\item{value}{array; the variable's data, with dimensions in the same
order as \code{dim_names}.}

\item{dim_names}{character; the name of each dimension of \code{value}, in
order (e.g. \code{c("nzones", "time")}).}

\item{dim_values}{named list; one element per entry in \code{dim_names},
giving the coordinate/index values along that dimension (e.g. zone
numbers, or a \code{Date} vector for a \code{"time"} dimension).}
}
\value{
An object of class \code{aeme_grouped_var}.
}
\description{
Some GLM-AED output variables have dimensions AEME has no fixed
convention for (e.g. \code{nzones}, \code{particle}, \code{sed_layers}, \code{lon}, \code{lat}),
unlike the package's usual \code{(time)}-vector / \verb{(z, time)}-matrix output
shapes. Rather than force such a variable into the depth x time
convention -- which would silently misinterpret the extra axis as depth
or time -- \code{\link[=read_glm_output]{read_glm_output()}} keeps it as its own labelled array via
this constructor, so the actual index/coordinate values for every
dimension are always available alongside the data, ready to be
interpreted properly later.
}
