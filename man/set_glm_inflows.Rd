% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_glm_inflows.R
\name{set_glm_inflows}
\alias{set_glm_inflows}
\title{Set inflow data for a GLM-AED simulation directory}
\usage{
set_glm_inflows(
  path_glm,
  list_inf,
  inf_factor = 1,
  mass = TRUE,
  glm_file = find_glm_nml(path_glm)
)
}
\arguments{
\item{path_glm}{filepath; to GLM-AED directory (containing the nml file
and a \verb{bcs/} subdirectory)}

\item{list_inf}{named list of data.frames, one per inflow. Each must have
a \code{Date} column plus flow/temperature/salt columns (e.g. \code{HYD_flow},
\code{HYD_temp}, \code{CHM_salt}) -- see \code{\link[=add_inflow]{add_inflow()}} for the expected schema.}

\item{inf_factor}{numeric; scaling factor applied to all inflow flow
rates. Default is \code{1}.}

\item{mass}{logical; convert inflow variables to GLM-AED mass units using
the package's built-in conversion table. Default is \code{TRUE}.}

\item{glm_file}{filepath; path to the GLM hydrodynamic nml file to
update. Defaults to the file found in \code{path_glm} via \code{\link[=find_glm_nml]{find_glm_nml()}}.}
}
\value{
invisibly, the updated nml object
}
\description{
Thin, \code{aeme}-free wrapper around the internal inflow writer used by
\code{\link[=build_aeme]{build_aeme()}}. Writes one boundary-condition csv per inflow into
\code{path_glm/bcs} and updates the \verb{&inflow} block of the GLM nml file to
point at them.
}
\examples{
\dontrun{
set_glm_inflows(path_glm, list_inf = list(stream1 = inflow_df))
}
}
