% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_gotm_outflows.R
\name{set_gotm_outflows}
\alias{set_gotm_outflows}
\title{Set outflow data for a GOTM-WET simulation directory}
\usage{
set_gotm_outflows(
  path_gotm,
  outf,
  outf_factor = 1,
  yaml_file = file.path(path_gotm, "gotm.yaml")
)
}
\arguments{
\item{path_gotm}{filepath; to GOTM-WET directory (containing \code{gotm.yaml}
and an \verb{inputs/} subdirectory)}

\item{outf}{named list of data.frames, one per outflow, each with a
\code{Date} column and a \code{HYD_flow} column -- see \code{\link[=add_outflows]{add_outflows()}} for the
expected schema.}

\item{outf_factor}{numeric; scaling factor applied to all outflow flow
rates. Default is \code{1}.}

\item{yaml_file}{filepath; path to the \code{gotm.yaml} file to update.
Defaults to \code{gotm.yaml} in \code{path_gotm}.}
}
\value{
invisibly, the updated yaml list
}
\description{
Thin, \code{aeme}-free wrapper around the internal outflow writer used by
\code{\link[=build_aeme]{build_aeme()}}. Writes \verb{inputs/outf_<name>.dat} per outflow into
\code{path_gotm}, and updates the \code{streams} block of \code{gotm.yaml} to point at
them. Existing stream entries not named in \code{outf} are left untouched.
}
\examples{
\dontrun{
set_gotm_outflows(path_gotm, outf = list(outlet_1 = outflow_df))
}
}
