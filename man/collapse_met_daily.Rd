% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_water_balance.R
\name{collapse_met_daily}
\alias{collapse_met_daily}
\title{Collapse a sub-daily meteo data frame to a daily time step}
\usage{
collapse_met_daily(obs_met, precip = c("mean", "sum"))
}
\arguments{
\item{obs_met}{data frame; meteo forcing with a \code{Date} column (\code{Date} or
\code{POSIXct}) and numeric \verb{MET_*} columns.}

\item{precip}{character; how to aggregate \code{MET_pprain} / \code{MET_ppsnow} over
the day. \code{"mean"} (default) when they are already a mm/day rate;
\code{"sum"} when they are per-time-step accumulations.}
}
\value{
A data frame with one row per day: \code{Date} (as \code{Date}) and the
day-aggregated numeric columns.
}
\description{
Aggregates a \code{POSIXct}-stamped (or otherwise sub-daily) meteorological data
frame to one row per calendar day: every numeric column is averaged over the
day, except \code{MET_pprain} / \code{MET_ppsnow}, whose aggregation is controlled by
\code{precip}. Non-numeric columns other than \code{Date} are dropped, and \code{Date} is
returned as a \code{Date}.
}
\details{
AEME defines \code{MET_pprain} / \code{MET_ppsnow} as a \strong{rate in mm/day}
(see \code{\link[=standardise_met]{standardise_met()}}). If the frame is already on that convention -- e.g.
the output of \code{\link[=standardise_met]{standardise_met()}}, which is what \code{\link[=build_aeme]{build_aeme()}} feeds the
water balance -- a daily \emph{mean} of the rate is the daily rate, so
\code{precip = "mean"} (the default) is correct. Raw sub-daily reanalysis / AWS
products instead report precipitation as an \strong{accumulation per time step}
(mm that fell during that step); for those, use \code{precip = "sum"} to get the
daily total.
}
\examples{
hourly <- data.frame(
  Date = seq(as.POSIXct("2020-01-01", tz = "UTC"), by = "hour",
             length.out = 48),
  MET_tmpair = rnorm(48, 15, 3),
  MET_pprain = c(rep(0, 20), rep(0.5, 4), rep(0, 24))  # mm per hour
)
collapse_met_daily(hourly, precip = "sum")
}
\seealso{
\code{\link[=standardise_met]{standardise_met()}}
}
