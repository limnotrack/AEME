% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardise_glm_output.R
\name{standardise_glm_output}
\alias{standardise_glm_output}
\title{Standardise a raw GLM-AED output list onto AEME's common depth grid}
\usage{
standardise_glm_output(out_raw, depths = NULL)
}
\arguments{
\item{out_raw}{list; an \code{aeme_output_raw}-classed GLM-AED output list
from \code{\link[=read_glm_output]{read_glm_output()}} with \code{raw_output = TRUE} (must include its
\code{z} and \code{LKE_lvlwtr} entries).}

\item{depths}{numeric vector; depths to interpolate onto. If \code{NULL}
(default), uses the same standardised depth-fraction grid
\code{read_glm_output(raw_output = FALSE)} uses (see
\link{model_layer_structure}).}
}
\value{
An \code{aeme_output}-classed list (see \code{\link[=is_aeme_output]{is_aeme_output()}}),
structurally the same as \code{read_glm_output(raw_output = FALSE)}'s
return value.
}
\description{
Interpolates every \verb{(z, time)} variable in a \code{read_glm_output(raw_output = TRUE)} list from GLM's own, time-varying layer structure -- captured in
its \code{z} entry (see \code{\link[=read_glm_output]{read_glm_output()}}) -- onto a shared depth grid, the
same one \code{raw_output = FALSE} produces, without re-reading the netCDF
file. Also renames variables back to their AEME \code{var_aeme} names (where
\link{key_naming} has a translation) and re-applies AED unit-conversion
factors, so the result resembles \code{read_glm_output(raw_output = FALSE)}'s output as closely as possible.
}
\details{
Useful when you've already loaded raw output (e.g. to inspect native
units/names) and only later decide you want it on AEME's standard grid
too -- interpolating in place is cheaper than re-opening the netCDF file
and reading it again with \code{raw_output = FALSE}.

\code{aeme_grouped_var} entries (dimensions other than \code{(time)}/\verb{(z, time)},
e.g. sediment-zone variables) are carried over unchanged -- they aren't
on a depth grid to begin with, so there's nothing to interpolate.
}
\examples{
\dontrun{
out_raw <- read_glm_output(file = outfile, raw_output = TRUE)
out_std <- standardise_glm_output(out_raw)
plot_model_output(out_std, "HYD_temp")
}
}
