% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_sed_temp.R
\name{calc_sed_temp}
\alias{calc_sed_temp}
\title{GLM sediment-temperature parameters from observed temperature profiles}
\usage{
calc_sed_temp(
  aeme = NULL,
  obs_temp = NULL,
  hypsograph = NULL,
  sed_zones = NULL,
  max_depth = NULL,
  temp_var = "HYD_temp",
  nml_file = "glm4.nml",
  output = c("parameters", "nml", "summary"),
  depth_grid = 0.25,
  depth_tol = 2,
  min_obs = 10,
  min_months = 6,
  default_mean = 12,
  default_amplitude = 4,
  default_peak_doy = 46,
  borrow_amp_factor = 0.6,
  borrow_lag_days = 25,
  verbose = TRUE
)
}
\arguments{
\item{aeme}{\code{Aeme} object; when supplied, \code{obs_temp} and \code{hypsograph} are
extracted from it unless passed explicitly. Default \code{NULL}.}

\item{obs_temp}{long df: Date, var_aeme, depth_from, depth_to, value (degC),
depths in m below surface. Optional when \code{aeme} is given.}

\item{hypsograph}{df with \code{depth} (m, +down) or \code{elev}, and \code{area} (m2), used
to area-weight within a zone and (with \code{sed_zones = NULL}) to derive the
zones. Optional when \code{aeme} is given. \code{NULL} -> uniform weighting and
\code{sed_zones} / \code{max_depth} must be supplied.}

\item{sed_zones}{numeric vector of zone upper-boundary heights above the bed,
ascending - exactly as passed to GLM \code{zone_heights}. Zone 1 is
0 -> sed_zones[1] (the deepest slab); the last value may exceed max depth.
\code{NULL} (default) -> \code{\link[=estimate_sed_zones]{estimate_sed_zones()}} on \code{hypsograph}.}

\item{max_depth}{max lake depth (m). NULL -> from hypsograph, else deepest obs (warns).}

\item{temp_var}{value of var_aeme to keep.}

\item{nml_file}{character; name of the GLM nml file the parameters belong to,
used for the \code{file} column of the parameter table. Default \code{"glm4.nml"}.}

\item{output}{one of \code{"parameters"} (default) - a long AEME model-parameter
table in \code{\link[=param_colnames]{param_colnames()}}\code{(incl_opt = FALSE)} order, ready for
calibration; \code{"nml"} - a named list of the three per-zone vectors to splice
into \code{glm_nml[["sediment"]]}; or \code{"summary"} - the per-zone diagnostic
data.frame with \code{parameters} / \code{nml} / \code{zone_series} attributes.}

\item{depth_grid}{within-zone integration step (m).}

\item{depth_tol}{a cast is used for a zone only if its deepest sample is within
depth_tol m of the top of that zone's depth band.}

\item{min_obs, min_months}{thresholds for a harmonic fit (else monthly-range).}

\item{default_mean, default_amplitude, default_peak_doy}{last-resort values.
default_peak_doy = 46 is mid-Feb (S. hemisphere summer); use ~209 for N.}

\item{borrow_amp_factor, borrow_lag_days}{when an unfitted deep zone borrows a
cycle from the nearest shallower fitted zone: damp amplitude by
borrow_amp_factor^(zone steps) and lag peak_doy by borrow_lag_days per step.}

\item{verbose}{logical; print the assembled per-zone table, nml snippet and
parameter table.}
}
\value{
Depends on \code{output}:
\itemize{
\item \code{"parameters"} - data.frame with columns \code{model}, \code{file}, \code{name},
\code{value}, \code{min}, \code{max}, \code{group}, \code{index}; one row per zone for each of
\code{sediment/sed_temp_mean}, \code{sediment/sed_temp_amplitude} and
\code{sediment/sed_temp_peak_doy}.
\item \code{"nml"} - named list \code{sed_temp_mean} / \code{sed_temp_amplitude} /
\code{sed_temp_peak_doy}, each a per-zone numeric vector in GLM zone order.
\item \code{"summary"} - the per-zone diagnostic data.frame (zone 1 = deepest),
carrying the \code{"parameters"} table, a ready-to-paste \code{"nml"} snippet and
the per-zone \code{"zone_series"} as attributes.
}
}
\description{
Fits an annual sediment-temperature cycle (\code{sed_temp_mean},
\code{sed_temp_amplitude}, \code{sed_temp_peak_doy}) for each GLM sediment zone from
observed water-column temperature profiles. Used internally by
\code{\link[=build_aeme]{build_aeme()}} to populate the GLM \verb{&sediment} block, and exported so the
same per-zone values can be generated as a calibration parameter table.
}
\details{
Inputs can be supplied directly (\code{obs_temp}, \code{hypsograph}, \code{sed_zones}) or
pulled from an \link[=Aeme-class]{Aeme} object via \code{aeme}: observations come from
\code{\link[=get_obs]{get_obs()}} and the hypsograph from \code{\link[=input]{input()}}. \code{sed_zones} defaults to
\code{\link[=estimate_sed_zones]{estimate_sed_zones()}} on the hypsograph when not given.
}
