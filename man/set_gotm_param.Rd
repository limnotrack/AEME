% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_gotm_param.R
\name{set_gotm_param}
\alias{set_gotm_param}
\title{Set one or more parameter values in a GOTM-WET \code{gotm.yaml} file}
\usage{
set_gotm_param(path_gotm, ..., yaml_file = file.path(path_gotm, "gotm.yaml"))
}
\arguments{
\item{path_gotm}{filepath; directory containing the GOTM-WET
configuration}

\item{...}{named parameter/value pairs to set, using a dot-separated path
into the nested yaml structure, e.g. \code{`time.dt` = 1800} or
\code{`location.latitude` = -36.9}. Values must be of the same type
(numeric, logical, character) as the current value in the file.}

\item{yaml_file}{filepath; path to the \code{gotm.yaml} file to edit. Defaults
to \code{gotm.yaml} in \code{path_gotm}.}
}
\value{
invisibly, the updated yaml list
}
\description{
Thin, \code{aeme}-free wrapper for editing a GOTM-WET \code{gotm.yaml} file in
place. Intended for a GOTM-WET-only workflow where a user just wants to
tweak parameters, run the model, and load the output.
}
\examples{
\dontrun{
set_gotm_param(path_gotm, `time.dt` = 1800)
}
}
