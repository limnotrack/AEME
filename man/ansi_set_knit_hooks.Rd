% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ansi_set_knit_hooks.R
\name{ansi_set_knit_hooks}
\alias{ansi_set_knit_hooks}
\title{Set knitr Hooks for ANSI Aware HTML Output}
\usage{
ansi_set_knit_hooks(
  hooks,
  which = c("output", "message", "warning", "error"),
  class = "ansi",
  style = TRUE,
  collapse = TRUE,
  palette = "vscode",
  colors = 256,
  preserve_newlines = FALSE
)
}
\arguments{
\item{hooks}{The \link[knitr:knit_hooks]{knitr::knit_hooks} object.}

\item{which}{Character vector of hooks to replace. Defaults to
\code{c("output", "message", "warning", "error")}.}

\item{class}{Character vector of CSS classes for output blocks, recycled
to match \code{which}. Defaults to \code{"ansi"}.}

\item{style}{Whether to emit CSS styles from \code{\link[cli:ansi_html_style]{cli::ansi_html_style()}}.
Defaults to \code{TRUE}.}

\item{collapse}{Whether to emit CSS to remove margins between consecutive
output blocks. Defaults to \code{TRUE}.}

\item{palette}{Palette for \code{\link[cli:ansi_html_style]{cli::ansi_html_style()}}. Defaults to \code{"vscode"}.}

\item{colors}{Number of ANSI colors to set via \code{\link[cli:num_ansi_colors]{cli::num_ansi_colors()}}.
Defaults to \code{256}. Set to \code{NULL} to leave unchanged.}

\item{preserve_newlines}{Whether to convert newlines to \verb{<br>} tags to
ensure they are preserved in the HTML output. Defaults to \code{FALSE}.
Set to \code{TRUE} if your output is missing line breaks.}
}
\value{
Invisibly returns the previous hooks as a named list.
}
\description{
Configures \link[knitr:knitr-package]{knitr} output hooks to convert ANSI
sequences to HTML. This enables styled \link[cli:cli]{cli} output to render correctly
in HTML documents produced by \link[knitr:knitr]{knitr}, \link[rmarkdown:rmarkdown]{R Markdown},
or \href{https://quarto.org/}{Quarto}.
}
\details{
The hook only processes output containing ANSI sequences (detected via
\code{\link[cli:ansi_has_any]{cli::ansi_has_any()}}). Other output is passed to the original hook unchanged.

For non-HTML output formats, ANSI sequences are stripped via \code{\link[cli:ansi_strip]{cli::ansi_strip()}}.
}
\seealso{
\code{\link[cli:ansi_html]{cli::ansi_html()}}, \code{\link[cli:ansi_html_style]{cli::ansi_html_style()}}, \code{\link[cli:ansi_strip]{cli::ansi_strip()}}
}
