% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_initial_conditions.R
\name{set_initial_conditions}
\alias{set_initial_conditions}
\title{Set initial conditions for AEME models}
\usage{
set_initial_conditions(
  aeme,
  depth = NULL,
  profile = NULL,
  wq = NULL,
  model_init = NULL,
  model_controls = NULL,
  from_obs = FALSE,
  reset = FALSE
)
}
\arguments{
\item{aeme}{Aeme object.}

\item{depth}{numeric(1); initial water depth (m), i.e. the height of the
lake surface above the lowest point of the hypsograph. \code{NULL} (default)
leaves it unchanged.}

\item{profile}{data.frame; initial profile with a \code{depth} column
(positive-down, 0 = surface) and a \code{temperature} column (degC); a \code{salt}
column (ppt) is optional and defaults to 0. \code{NULL} (default) leaves the
profile unchanged.}

\item{wq}{named list; initial water-column values keyed by AEME variable
name (e.g. \code{list(CHM_oxy = 300, NIT_amm = 0.5)}). Each element is either
a single number (constant with depth) or a data.frame with \code{depth} and
\code{value} columns. Names must be present in \code{model_controls$var_aeme}.
\code{NULL} (default) leaves water-quality initial values unchanged.}

\item{model_init}{named list; per-model overrides keyed by model name
(\code{"dy_cd"}, \code{"glm_aed"}, \code{"gotm_wet"}, \code{"simstrat_aed2"},
\code{"simstrat_aed"}). Each element is itself a list with any of \code{depth},
\code{profile} and \code{wq}, following the same rules as the generic arguments.
These are merged over the generic specification when that model is built.
\code{NULL} (default) leaves per-model overrides unchanged.}

\item{model_controls}{data.frame; model configuration, typically loaded via
\code{\link[=get_model_controls]{get_model_controls()}}.}

\item{from_obs}{logical; if \code{TRUE}, seed the generic profile and
water-quality values from lake observations via \code{\link[=update_init]{update_init()}} before
the explicit arguments are applied (the explicit arguments take
precedence). Default \code{FALSE}.}

\item{reset}{logical; if \code{TRUE}, discard any existing
\code{configuration(aeme)$initial_conditions} before applying the arguments.
Default \code{FALSE}, i.e. new values are merged into the existing
specification.}
}
\value{
The \code{aeme} object with initial conditions set.
}
\description{
Configure the initial state - water depth, temperature and salinity
profile, and biogeochemical water-column pools - that \code{\link[=build_aeme]{build_aeme()}} writes
into each model's configuration. Values can be supplied once for every
model (\code{depth}, \code{profile}, \code{wq}) and/or overridden per model via
\code{model_init} (for example a GLM-AED-specific temperature profile).
}
\details{
The structured specification is stored in
\code{configuration(aeme)$initial_conditions} as a list with a \code{default} entry
plus an optional entry per model, and is resolved at build time by merging
each model's overrides over the defaults. For backwards compatibility the
generic \code{depth}/\code{profile} are also written to \code{input(aeme)$init_depth} /
\code{input(aeme)$init_profile}, and scalar generic \code{wq} values into
\code{configuration(aeme)$model_controls$initial_wc}, so the generic controls
take effect immediately.
}
\examples{
aeme_file <- system.file("extdata/aeme.rds", package = "AEME")
aeme <- readRDS(aeme_file)

# Generic profile for all models
prof <- data.frame(depth = c(0, 5, 10), temperature = c(18, 14, 11))
aeme <- set_initial_conditions(aeme, depth = 10, profile = prof)

# GLM-AED-specific override
aeme <- set_initial_conditions(
  aeme,
  model_init = list(
    glm_aed = list(profile = data.frame(depth = c(0, 10),
                                        temperature = c(20, 12)))
  )
)
}
\seealso{
\code{\link[=get_initial_conditions]{get_initial_conditions()}}, \code{\link[=update_init]{update_init()}}, \code{\link[=set_glm_init]{set_glm_init()}},
\code{\link[=set_gotm_init]{set_gotm_init()}}, \code{\link[=set_simstrat_init]{set_simstrat_init()}}
}
