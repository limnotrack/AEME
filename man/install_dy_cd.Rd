% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_dy_cd.R
\name{install_dy_cd}
\alias{install_dy_cd}
\title{Install DYRESM-CAEDYM executables for AEME}
\usage{
install_dy_cd(
  version = "latest",
  os = NULL,
  repo = "limnotrack/AEME",
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{version}{Character. The DYRESM-CAEDYM version to install, e.g.
\code{"5.0.0"}. Use \code{\link[=list_dy_cd_versions]{list_dy_cd_versions()}} to see what's available.
Defaults to \code{"latest"}, which resolves to the highest version number
available for the current platform.}

\item{os}{Character. One of \code{"windows"}, \code{"macos"}, or \code{"linux"}.
Defaults to the platform R is currently running on; you shouldn't
normally need to set this. Note that only a Windows build exists (no
source is available to build for other platforms), so \code{macos}/\code{linux}
will report no versions available.}

\item{repo}{Character. The \code{"owner/repo"} GitHub repository that
DYRESM-CAEDYM binaries are attached to. Defaults to
\code{"limnotrack/AEME"}.}

\item{force}{Logical. If \code{FALSE} (the default) and this version is
already installed for this platform, the download/verification steps
are skipped and the existing path is returned. Set to \code{TRUE} to
re-download and reinstall anyway.}

\item{quiet}{Logical. If \code{TRUE}, suppresses progress messages (download
errors and checksum failures still raise, and are never silenced).}
}
\value{
Invisibly, the file path to the installed \code{dycd.exe} (its three
companion tools sit alongside it in the same directory).
}
\description{
Downloads the pre-compiled DYRESM-CAEDYM executable suite (\code{dycd.exe}
plus its three companion tools, \code{createDYref.exe}, \code{createDYsim.exe}, and
\code{extractDYinfo.exe}) for the current platform, verifies it against its
published SHA256 checksum, and installs it into a persistent user cache
directory. Binaries are attached as assets to AEME's GitHub releases,
with the version encoded in the asset filename (e.g.
\verb{dycd-windows-5.0.0.zip}), mirroring \code{\link[=install_glm_aed]{install_glm_aed()}}.
}
\details{
DYRESM-CAEDYM is closed-source (no public repository to build from), so
unlike GLM there is no possibility of compiling it in a CI pipeline --
this only ever distributes whatever binary was previously obtained and
vetted, exactly as the binaries bundled directly in earlier AEME versions
were.

Binaries are cached under \code{\link[tools:R_user_dir]{tools::R_user_dir("AEME", "data")}},
in an \verb{<os>/<version>/} subdirectory (the same root \code{\link[=install_glm_aed]{install_glm_aed()}},
\code{\link[=install_gotm_wet]{install_gotm_wet()}}, and \code{\link[=install_simstrat_aed2]{install_simstrat_aed2()}} use). Every binary is
published alongside a \code{.sha256} checksum file in the same release. This
function downloads both, recomputes the SHA256 of the downloaded zip
locally, and compares it to the published value before extracting
anything. If the checksums don't match, or if no checksum file is found
at all, installation is aborted and nothing is extracted -- this function
will not install an unverified binary under any circumstances.
}
\examples{
\dontrun{
install_dy_cd(version = "5.0.0")

# Force re-download and reinstall
install_dy_cd(version = "5.0.0", force = TRUE)
}

}
\seealso{
\code{\link[=list_dy_cd_versions]{list_dy_cd_versions()}} to discover available versions,
\code{\link[=dy_cd_exe_path]{dy_cd_exe_path()}} to locate an already-installed executable,
\code{\link[=install_glm_aed]{install_glm_aed()}} for the equivalent GLM-AED installer this mirrors.
}
