% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_model_init.R
\name{set_gotm_init}
\alias{set_gotm_init}
\title{Set initial conditions for a GOTM-WET simulation}
\usage{
set_gotm_init(
  path_gotm,
  temp = NULL,
  salt = NULL,
  gotm_file = file.path(path_gotm, "gotm.yaml")
)
}
\arguments{
\item{path_gotm}{filepath; directory containing the GOTM-WET configuration}

\item{temp}{numeric; new initial water temperature profile. Recycled to
the number of depths in \code{inputs/t_prof_file.dat}. \code{NULL} (default) leaves
it unchanged.}

\item{salt}{numeric; new initial salinity profile, same recycling rule as
\code{temp}, written to \code{inputs/s_prof_file.dat}. \code{NULL} (default) leaves it
unchanged.}

\item{gotm_file}{filepath; path to the yaml file to edit. Defaults to
\code{gotm.yaml} in \code{path_gotm}.}
}
\value{
invisibly, the updated gotm yaml object
}
\description{
Thin wrapper for editing the initial temperature/salinity profile files
(\code{inputs/t_prof_file.dat}/\code{inputs/s_prof_file.dat}) of a GOTM-WET model
directory in place, without needing an \code{aeme} object. Intended for a
GOTM-WET-only workflow where a user just wants to tweak initial
conditions, run the model, and load the output.
}
\details{
The existing profile depths in each \code{.dat} file are left unchanged --
\code{temp}/\code{salt} values are recycled (via \code{\link[=rep_len]{rep_len()}}) across however many
depths are already defined. The \code{gotm.yaml} surface SST seed
(\code{surface$sst$constant_value}) is updated to match the surface-most
\code{temp} value when \code{temp} is provided.
}
\examples{
\dontrun{
set_gotm_init(path_gotm, temp = seq(20, 10, length.out = 10))
}
}
