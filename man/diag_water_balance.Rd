% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag_aeme.R
\name{diag_water_balance}
\alias{diag_water_balance}
\title{Water level bias and drift, against observations and against the
model's own starting point}
\usage{
diag_water_balance(
  aeme,
  model,
  drift_ok_m_per_yr = 0.05,
  bias_ok_m = NULL,
  min_n = 20
)
}
\arguments{
\item{aeme}{Aeme object; already built and run for \code{model} (see
\code{\link{build_aeme}}/\code{\link{run_aeme}}).}

\item{model}{character; single model code to diagnose (see
\code{\link{check_model}}).}

\item{drift_ok_m_per_yr}{numeric; below this absolute slope, classified
\code{"static_offset"} (or \code{"ok"}) rather than \code{"drift"}.}

\item{bias_ok_m}{numeric or \code{NULL}; below this absolute mean bias (in
metres), classified \code{"ok"} regardless of drift. When \code{NULL} (default),
derived from the lake's maximum depth as \verb{5\%} of max depth, clamped to
\eqn{[0.1, 0.5]} m, so the threshold scales sensibly across shallow and
deep lakes without changing the absolute-unit semantics.}

\item{min_n}{integer; below this many observations, the
obs-based signal is not trusted (even if computed) and the model-only
signal decides the classification instead.}
}
\value{
list with \code{mean_bias}, \code{drift_m_per_yr}, \code{n}, \code{span_yr},
\code{sim_drift_m_per_yr}, \code{sim_change_m}, \code{sim_span_yr}, \code{classification}
(one of \code{"ok"}, \code{"static_offset"}, \code{"drift"}, \code{"ok_no_obs"},
\code{"sim_drift"}, \code{"no_data"}), \code{low_confidence}, \code{detail}.
}
\description{
Reports two independent drift signals:
\itemize{
\item{\code{drift_m_per_yr}}{ - obs-based: slope of the (sim - obs)
residual over time, at whatever observed dates exist. Needs real
observations, and enough of them to trust a slope.}
\item{\code{sim_drift_m_per_yr}}{ - model-only: slope of the raw
simulated \code{LKE_lvlwtr} series itself (\code{\link{get_var}} with
\code{use_obs = FALSE}, i.e. referenced to the hypsography's own zero,
not any observation-derived elevation offset) against time, over every
simulated day in the post-spin-up window. Available for every lake
regardless of observation coverage, so a lake that is silently draining
or filling under its own simulated water balance shows up here even with
zero level observations to compare against.}
}
}
