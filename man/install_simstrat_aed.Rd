% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_simstrat_aed.R
\name{install_simstrat_aed}
\alias{install_simstrat_aed}
\title{Install a Simstrat-AED executable for AEME}
\usage{
install_simstrat_aed(
  version = "latest",
  os = NULL,
  repo = "limnotrack/AEME",
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{version}{Character. The Simstrat version to install, e.g.
\code{"3.0.4"}. Use \code{\link[=list_simstrat_aed_versions]{list_simstrat_aed_versions()}} to see what's available.
Defaults to \code{"latest"}, which resolves to the highest version number
available for the current platform.}

\item{os}{Character. One of \code{"windows"}, \code{"macos"}, or \code{"linux"}.
Defaults to the platform R is currently running on; you shouldn't
normally need to set this.}

\item{repo}{Character. The \code{"owner/repo"} GitHub repository that Simstrat
binaries are attached to. Defaults to \code{"limnotrack/AEME"}.}

\item{force}{Logical. If \code{FALSE} (the default) and this version is
already installed for this platform, the download/verification steps
are skipped and the existing path is returned. Set to \code{TRUE} to
re-download and reinstall anyway.}

\item{quiet}{Logical. If \code{TRUE}, suppresses progress messages (download
errors and checksum failures still raise, and are never silenced).}
}
\value{
Invisibly, the file path to the installed Simstrat-AED
executable.
}
\description{
Downloads a pre-compiled Simstrat (coupled with AED, not AED2) binary for
the current platform, verifies it against its published SHA256 checksum,
and installs it into a persistent user cache directory. Mirrors
\code{\link{install_simstrat_aed2}} exactly, except for the release-asset
filename prefix (\verb{simstrat_aed-*} here vs \verb{simstrat_aed2-*} for AED2, as
built by \code{.github/workflows/build-simstrat.yaml}) and the cache
subdirectory it installs into, so an AED2 and an AED build can be
installed side by side without colliding.
}
\details{
Binaries are cached under \code{\link[tools:R_user_dir]{tools::R_user_dir("AEME", "data")}},
in an \verb{<os>/simstrat_aed/<version>/} subdirectory -- distinct from
Simstrat-AED2's \verb{<os>/<version>/} root so the two coexist without the
installed \code{simstrat}/\code{simstrat.exe} filename colliding for the same
version string.

Every binary is published alongside a \code{.sha256} checksum file in the same
release. This function downloads both, recomputes the SHA256 of the
downloaded zip locally, and compares it to the published value before
extracting anything. If the checksums don't match, or if no checksum file
is found at all, installation is aborted and nothing is extracted -- this
function will not install an unverified binary under any circumstances.
}
\examples{
\dontrun{
install_simstrat_aed(version = "3.0.4")

# Force re-download and reinstall
install_simstrat_aed(version = "3.0.4", force = TRUE)
}

}
\seealso{
\code{\link[=list_simstrat_aed_versions]{list_simstrat_aed_versions()}} to discover available versions,
\code{\link[=simstrat_aed_exe_path]{simstrat_aed_exe_path()}} to locate an already-installed executable,
\code{\link[=install_simstrat_aed2]{install_simstrat_aed2()}} for the AED2 installer this mirrors.
}
