% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_simstrat_dat.R
\name{read_simstrat_dat_file}
\alias{read_simstrat_dat_file}
\title{Read a single Simstrat \verb{<var>_out.dat} output file}
\usage{
read_simstrat_dat_file(file, skip_rows = 0L, n_rows = -1L)
}
\arguments{
\item{file}{character; path to a \verb{<var>_out.dat} file.}

\item{skip_rows}{integer; number of \emph{data} rows (after the header) to skip.
Lets a caller read only the part of a file it needs, rather than parsing
rows it will immediately discard. Default \code{0}.}

\item{n_rows}{integer; number of data rows to read after \code{skip_rows}, or
\code{-1} (default) for all remaining rows.}
}
\value{
List with elements
\describe{
\item{\code{day}}{numeric vector of Simstrat day numbers, one per row read.}
\item{\code{depths}}{numeric vector of the header's depths/zone heights, one
per value column (length 1 for a surface/whole-lake variable). As
written by Simstrat: negative-down offsets from the lake surface for
water-column variables, positive heights above the lake bottom for
\verb{_zone} variables.}
\item{\code{values}}{numeric matrix, \code{length(day)} rows x \code{length(depths)}
columns, in the file's own column order.}
\item{\code{offset}}{integer; \code{skip_rows}, so a caller can map row indices in
the full file onto rows of \code{values}.}
}
}
\description{
Simstrat writes one plain-text file per output variable (see
\code{strat_outputfile.f90::open_files()} in the Simstrat source): a header row
of \verb{Datetime,<z1>,<z2>,...} -- depths (or, for \verb{_zone_out.dat} files, zone
heights) written with Fortran \code{F12.3}, a single trailing column for
surface/whole-lake variables -- followed by one \code{(F12.4)} day number plus
\code{(ES14.4E3)} values per output time.
}
\details{
Everything in the file is numeric, so it is read with a single
\code{\link[base]{scan}} call rather than \code{\link[utils]{read.csv}} --
substantially faster on the large depth x time files, which matters when a
calibration reads the output once per model run (see
\code{\link{read_simstrat_dat}}).
}
