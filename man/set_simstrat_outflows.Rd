% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_simstrat_outflows.R
\name{set_simstrat_outflows}
\alias{set_simstrat_outflows}
\title{Set outflow data for a Simstrat-AED2 simulation directory}
\usage{
set_simstrat_outflows(
  path_simstrat,
  outf,
  heights_wdr,
  surface_elev,
  outf_factor = 1,
  ref_year = NULL,
  par_file = file.path(path_simstrat, "simstrat.par")
)
}
\arguments{
\item{path_simstrat}{filepath; to Simstrat-AED2 directory}

\item{outf}{named list of data.frames, one per outflow, each with a
\code{Date} column and a \code{HYD_flow} column -- see \code{\link[=add_outflows]{add_outflows()}} for the
expected schema.}

\item{heights_wdr}{named numeric vector; withdrawal elevation (m,
absolute -- the same datum as \code{hyps$elev}/\code{surface_elev}) for each name
in \code{outf}.}

\item{surface_elev}{numeric; the lake surface elevation (m) that this
Simstrat-AED2 configuration was built with -- the zero-point for
\code{Bathymetry.dat}/\code{InitialConditions.dat}/inflow-outflow depths (see
\code{make_stg_simstrat()}). Unlike GLM's nml, Simstrat's own files only store
depths already relative to this elevation, so it cannot be recovered from
\code{path_simstrat} alone and must be supplied.}

\item{outf_factor}{numeric; scaling factor applied to all outflow flow
rates. Default is \code{1}.}

\item{ref_year}{integer; Simstrat's \verb{Simulation.Reference year}. Defaults
to the value already in \code{simstrat.par}.}

\item{par_file}{filepath; path to the \code{simstrat.par} file to read
\code{use_bgc}/\code{ref_year} defaults from. Defaults to \code{simstrat.par} in
\code{path_simstrat}.}
}
\value{
invisibly, \code{NULL}
}
\description{
Thin, \code{aeme}-free wrapper around the internal outflow writer used by
\code{\link[=build_aeme]{build_aeme()}}. Writes a combined \code{Qout.dat} into \code{path_simstrat}. All
outflows are summed into a single series at a single representative
withdrawal elevation, matching \code{make_wdr_simstrat()}'s existing
behaviour.
}
\examples{
\dontrun{
set_simstrat_outflows(path_simstrat, outf = list(outlet_1 = outflow_df),
                      heights_wdr = c(outlet_1 = 10.5), surface_elev = 13.07)
}
}
