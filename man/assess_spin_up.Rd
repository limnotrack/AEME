% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_spin_up.R
\name{assess_spin_up}
\alias{assess_spin_up}
\title{Assess model sensitivity to initial conditions and required spin-up}
\usage{
assess_spin_up(
  aeme,
  model,
  spin_up = c(0, 30, 90, 180, 365),
  perturb = list(temperature = c(-3, 0, 3)),
  vars = "HYD_temp",
  metric = c("spread", "spread_cv", "drift", "drift_cv"),
  model_controls = NULL,
  path = tempdir(),
  tolerance = NULL,
  build_args = list(),
  run_args = list(),
  verbose = FALSE
)
}
\arguments{
\item{aeme}{Aeme object.}

\item{model}{character(1); the model to assess.}

\item{spin_up}{numeric; spin-up lengths in days to test. Default
\code{c(0, 30, 90, 180, 365)}.}

\item{perturb}{named list describing the initial-condition ensemble (see
Details). Default \code{list(temperature = c(-3, 0, 3))}.}

\item{vars}{character; AEME variable names to report and restrict written
model output to. Default \code{"HYD_temp"}.}

\item{metric}{character(1); which summary column the recommendation (and
\code{tolerance}) applies to. One of \code{"spread"} (default; across-member SD in
the variable's units), \code{"spread_cv"} (that SD divided by the ensemble
mean, per depth - dimensionless, comparable across variables),
\code{"drift"} or \code{"drift_cv"} (the same normalisation applied to the
distance from the longest-spin-up ensemble mean). All four are always
present in \code{summary}; \code{metric} only selects the one used for
\code{recommended}. The \verb{*_cv} forms are meant for strictly-positive
variables (water-quality concentrations, salinity, K); a CV is
ill-defined where the ensemble mean crosses zero and those depths are
dropped from the depth-average.}

\item{model_controls}{data.frame; model configuration, typically loaded via
\code{\link[=get_model_controls]{get_model_controls()}}.}

\item{path}{character; directory under which the per-run subdirectories are
created. Default \code{tempdir()}.}

\item{tolerance}{numeric(1); threshold on \code{metric} (a named vector, one
per variable, is allowed) used to pick the recommended spin-up. \code{NULL}
(default) skips the recommendation.}

\item{build_args, run_args}{lists of extra arguments passed to
\code{\link[=build_aeme]{build_aeme()}} and \code{\link[=run_aeme]{run_aeme()}} respectively.}

\item{verbose}{logical; print model progress. Default \code{FALSE}.}
}
\value{
A list of class \code{aeme_spin_up} with:
\itemize{
\item \code{data} - long data.frame: \code{spin_up}, \code{member}, \code{var}, \code{depth}, \code{value}
(state at the start of the analysis period).
\item \code{summary} - \code{spin_up}, \code{var}, and, each as a mean over depth of the
per-depth quantity: \code{spread} (across-member SD), \code{spread_cv}
(SD / |ensemble mean|), \code{drift} (|this spin-up's mean - longest
spin-up's mean|) and \code{drift_cv} (that / |longest spin-up's mean|).
\item \code{recommended} - named numeric (per \code{var}) shortest spin-up whose
\code{metric} meets \code{tolerance}, and \code{overall} the maximum of those; \code{NULL}
if \code{tolerance} is \code{NULL}.
\item \code{model}, \code{vars}, \code{metric}, \code{tolerance}, \code{failures}.
}
}
\description{
Build and run an ensemble of simulations that differ only in their initial
conditions, repeated across a range of spin-up lengths, then measure how
the ensemble spread at the start of the analysis period collapses as
spin-up is extended. The shortest spin-up at which the spread falls below
\code{tolerance} is the length beyond which the choice of initial condition no
longer materially affects the reported variables.
}
\details{
Each ensemble member is a perturbation of the baseline initial conditions
(\code{get_initial_conditions(aeme, model = model)}, falling back to
\code{input(aeme)$init_profile} / \code{model_controls$initial_wc}). \code{perturb} is a
named list:
\itemize{
\item \code{"temperature"} / \code{"salt"} - numeric vector of \strong{additive} offsets
applied to that column of the initial profile.
\item any \code{model_controls$var_aeme} name - numeric vector of \strong{multiplicative}
factors applied to that water-quality initial value.
}

The vectors are recycled to a common length, which becomes the ensemble
size; member \code{i} takes element \code{i} of every vector.

One \code{\link[=build_aeme]{build_aeme()}} + \code{\link[=run_aeme]{run_aeme()}} is performed per (spin-up, member)
combination, each in its own subdirectory of \code{path}, with written output
restricted to \code{vars}. This can be a large number of model runs.
}
\examples{
\dontrun{
aeme_dir <- system.file("extdata/lake/", package = "AEME")
aeme <- yaml_to_aeme(path = aeme_dir, "aeme.yaml")
res <- assess_spin_up(aeme, model = "glm_aed",
                      spin_up = c(0, 30, 90, 180),
                      perturb = list(temperature = c(-4, 0, 4)),
                      tolerance = 0.5)
res$recommended
plot_spin_up(res)
}
}
\seealso{
\code{\link[=set_initial_conditions]{set_initial_conditions()}}, \code{\link[=plot_spin_up]{plot_spin_up()}}
}
