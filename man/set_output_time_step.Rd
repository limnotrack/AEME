% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_output_time_step.R
\name{set_output_time_step}
\alias{set_output_time_step}
\title{Set the model output temporal frequency}
\usage{
set_output_time_step(aeme, frequency)
}
\arguments{
\item{aeme}{An \link[=Aeme-class]{Aeme} object.}

\item{frequency}{The desired output frequency. Either a single positive
number of \strong{seconds}, or a character string:
\itemize{
\item a named frequency: \code{"subdaily"}/\code{"hourly"} (3600 s), \code{"daily"}
(86400 s), \code{"weekly"} (604800 s);
\item a \code{"<n> <unit>"} string, e.g. \code{"6 hours"}, \code{"30 min"}, \code{"1 day"},
\code{"900 sec"} (units: \code{sec}/\code{min}/\code{hour}/\code{day}/\code{week}, singular or
plural).
}}
}
\value{
The \code{aeme} object with \code{time(aeme)$output_time_step} updated. The
value must be greater than or equal to \code{time(aeme)$time_step}; \code{\link[=set_time]{set_time()}}
raises \code{aeme_error_output_time_step} otherwise.
}
\description{
A thin, generic wrapper around \code{\link[=set_time]{set_time()}} for the common case of only
wanting to change how often every model in the ensemble writes output. The
frequency applies to all models: \code{build_glm()}, \code{build_gotm()} and
\code{build_simstrat()} each translate \code{output_time_step} into their native
output-cadence setting (\code{nsave}, \code{output.yaml} \code{time_step}, and
\code{Output/Times} respectively), and the internal \code{aeme_time_axis()}
reconstructs the matching output time axis when the results are read back.
}
\details{
AEME does not temporally disaggregate forcing, so requesting output more
frequent than daily is only meaningful when the meteorological (and, where
relevant, inflow/outflow) forcing is supplied at least as often. See
\code{vignette("hourly-vs-daily-met")}.
}
\examples{
aeme <- readRDS(system.file("extdata/aeme.rds", package = "AEME"))
aeme <- set_output_time_step(aeme, "hourly")
time(aeme)$output_time_step
aeme <- set_output_time_step(aeme, "6 hours")
time(aeme)$output_time_step
}
\seealso{
\code{\link[=set_time]{set_time()}}
}
