% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_aeme.R
\name{new_aeme}
\alias{new_aeme}
\title{Create a new, minimal Aeme object}
\usage{
new_aeme(
  name = "newlake",
  id = "0001",
  latitude = 0,
  longitude = 0,
  elevation = 100,
  depth = 10,
  area = 1e+05,
  start = Sys.Date() - 365,
  stop = Sys.Date(),
  time_step = 3600,
  Kw = 1
)
}
\arguments{
\item{name}{character; lake name (alphanumeric only). Default \code{"newlake"}.}

\item{id}{character; lake identifier (alphanumeric only). Default
\code{"0001"}.}

\item{latitude}{numeric; lake latitude, in \eqn{[-90, 90]}. Default \code{0}.}

\item{longitude}{numeric; lake longitude, in \eqn{[-180, 180]}. Default
\code{0}.}

\item{elevation}{numeric; lake surface elevation above sea level (m).
Default \code{100}.}

\item{depth}{numeric; lake depth (m). Default \code{10}.}

\item{area}{numeric; lake surface area (m^2). Default \code{1e5}.}

\item{start}{character, Date, or POSIXct; simulation start date. Default
one year before \code{stop}.}

\item{stop}{character, Date, or POSIXct; simulation stop date. Default
today.}

\item{time_step}{numeric; time step in seconds. Default \code{3600} (1 hour).}

\item{Kw}{numeric; light extinction coefficient (m^-1). Default \code{1}.}
}
\value{
A valid \code{Aeme} object populated with placeholder values.
}
\description{
Returns a valid, fully-populated \code{Aeme} object built from placeholder
values, intended as a starting point for a new lake configuration.
Unlike \code{\link[=aeme_constructor]{aeme_constructor()}}, which requires real lake, time, and input
data and aborts without it, \code{new_aeme()} fills in sensible defaults so
you get a live object back immediately, ready to be built up
incrementally with the slot setters (e.g. \verb{lake<-}, \verb{time<-}, \verb{input<-})
and helpers such as \code{\link[=add_hypsograph]{add_hypsograph()}}, \code{\link[=add_met]{add_met()}}, and \code{\link[=add_inflows]{add_inflows()}}.
}
\examples{
aeme <- new_aeme()

aeme <- new_aeme(name = "mylake", id = "001", latitude = -37.8,
                 longitude = 175.3, elevation = 30, depth = 15,
                 area = 2.5e5)
}
\seealso{
\code{\link[=aeme_constructor]{aeme_constructor()}} for building an \code{Aeme} object from real
lake data with full validation.
}
