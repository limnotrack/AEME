% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_dy_cd_init.R
\name{set_dy_cd_init}
\alias{set_dy_cd_init}
\title{Set initial conditions for a DYRESM-CAEDYM simulation}
\usage{
set_dy_cd_init(
  path_dy,
  temp = NULL,
  salt = NULL,
  wq_init = NULL,
  pro_file = NULL,
  int_file = NULL
)
}
\arguments{
\item{path_dy}{filepath; directory containing the DYRESM-CAEDYM
configuration (the \code{dy_cd} model directory).}

\item{temp}{numeric; new initial water temperature profile. Recycled to
the number of rows in \verb{<lakename>.pro}. \code{NULL} (default) leaves it
unchanged.}

\item{salt}{numeric; new initial salinity profile, same recycling rule
as \code{temp}. \code{NULL} (default) leaves it unchanged.}

\item{wq_init}{named list; new water-column initial values for CAEDYM
water quality variables, keyed by \code{var_aeme} name, e.g.
\code{list(CHM_oxy = 300, NIT_amm = 0.5)}. Names are translated to CAEDYM's
own variable names via \code{rename_modelvars()}. Each value is a single
number (CAEDYM \code{.int} water-column initials are not depth-resolved).
\code{NULL} (default) leaves water quality initial values unchanged. Requires
a \verb{<lakename>.int} file (a CAEDYM configuration).}

\item{pro_file, int_file}{filepath; the \code{.pro} / \code{.int} files to edit.
Default to the files found in \code{path_dy} via \code{\link[=find_dy_cd_cfg]{find_dy_cd_cfg()}}'s prefix.}
}
\value{
invisibly, the updated initial-profile data.frame
(\code{depth}/\code{temp}/\code{salt}).
}
\description{
Thin, \code{aeme}-free wrapper for editing the initial temperature/salinity
profile (\verb{<lakename>.pro}) and, for a CAEDYM (BGC) run, the water-quality
initial values (\verb{<lakename>.int}) of a DYRESM-CAEDYM model directory in
place. Intended for a DYRESM-CAEDYM-only workflow where a user just wants
to tweak initial conditions, run the model, and load the output.
}
\details{
Existing profile depths in \verb{<lakename>.pro} are left unchanged --
\code{temp}/\code{salt} values are recycled (via \code{\link[=rep_len]{rep_len()}}) across however many
rows are already defined. \code{wq_init} overwrites the water-column initial
value for the named CAEDYM variables in \verb{<lakename>.int}, leaving their
sediment initial values (and every other variable) untouched.
}
\examples{
\dontrun{
set_dy_cd_init(path_dy, temp = seq(20, 10, length.out = 10))
set_dy_cd_init(path_dy, wq_init = list(CHM_oxy = 300, NIT_amm = 0.5))
}
}
