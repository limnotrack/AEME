% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_simstrat_dat.R
\name{read_simstrat_dat}
\alias{read_simstrat_dat}
\title{Read Simstrat's raw text output directly}
\usage{
read_simstrat_dat(
  sim_folder = NULL,
  vars_sim = NULL,
  depths = NULL,
  dates = NULL,
  date_index = NULL,
  incl_fluxes = TRUE,
  load_all = FALSE,
  raw_output = FALSE,
  model = "simstrat_aed2",
  config_file = "simstrat.par",
  out_dir = NULL,
  ref_year = NULL
)
}
\arguments{
\item{sim_folder}{character; path to the \code{simstrat_aed2}/\code{simstrat_aed}
simulation directory (containing \code{simstrat.par} and the output directory
it points at). Not needed if both \code{out_dir} and \code{ref_year} are supplied.}

\item{vars_sim}{Variables to extract in the AEME format e.g. "HYD_temp"}

\item{depths}{Depths to extract. If NULL, extract all model layer depths.
Defaults to NULL.}

\item{dates}{Dates to extract. If NULL, extract all dates. Defaults to NULL.}

\item{date_index}{Date index to extract. If NULL, extract all dates. Defaults
to NULL.}

\item{incl_fluxes}{Logical indicating whether to include flux variables.
Defaults to TRUE.}

\item{load_all}{logical; also load every other variable present in the
output directory beyond the declared \code{vars_sim} set. Default \code{FALSE} --
the opposite of \code{\link{read_simstrat_output}}'s default, because
loading every variable means reading every file, which is the cost this
function exists to avoid.}

\item{raw_output}{logical; if \code{TRUE}, return output as close to the raw
netCDF file as possible instead of AEME's standardised format: \verb{(z, time)} variables are left on GLM's own, time-varying model layer
midpoints (no interpolation onto a common depth grid), variables
requested via \code{vars_sim} are keyed by their raw GLM/netCDF name (e.g.
\code{"temp"}) rather than the translated AEME \code{var_aeme} name (e.g.
\code{"HYD_temp"}), and no AED unit-conversion factors are applied.
\code{depths} must not be supplied when \code{raw_output = TRUE}, since raw
output has no common depth grid to interpolate onto. Default \code{FALSE}.}

\item{model}{character; which Simstrat coupling this output came from,
\code{"simstrat_aed2"} (default) or \code{"simstrat_aed"}. Selects the matching
\code{key_naming} column for variable-name translation -- the netCDF file
format itself (produced by \code{\link{write_simstrat_nc}}) is identical
either way.}

\item{config_file}{character; name of (or path to) the Simstrat
configuration file. Default \code{"simstrat.par"}.}

\item{out_dir}{character; the output directory itself, skipping the
\code{simstrat.par} lookup. Default \code{NULL} (read from \code{simstrat.par}).}

\item{ref_year}{integer; the simulation's \verb{Simulation.Reference year},
skipping the \code{simstrat.par} lookup. Default \code{NULL} (read from
\code{simstrat.par}). Supply both this and \code{out_dir} in a tight loop to avoid
re-parsing the configuration on every read.}
}
\value{
List with AEME output variables, classed as for
\code{\link{read_simstrat_output}}, or a \code{model_output_error} (see
\code{\link{is_model_error}}) if the output directory holds no readable
output.
}
\description{
Reads Simstrat's own \verb{<var>_out.dat} text files straight from the
simulation's output directory, returning the same output list as
\code{\link{read_simstrat_output}} does from the consolidated \code{output.nc}
-- same keys, same depth grid, same unit conversions -- without going
through netCDF at all.
}
\details{
This is the fast path for workflows that read model output once per model
run, such as \code{aemetools}' calibration and sensitivity analyses. Reading the
text directly avoids both the netCDF write (which serialises \emph{every}
output variable, compressed) and the netCDF read, and only the files
actually asked for are parsed: with \code{load_all = FALSE} (the default here,
unlike \code{\link{read_simstrat_output}}) a single-variable read touches
one \verb{<var>_out.dat} file plus the small \code{WaterH_out.dat}, however many
variables the run wrote. When \code{date_index}/\code{dates} select a window of the
simulation, only rows up to the end of that window are parsed.

Simstrat writes every water-column variable on the same output depth grid
(\code{Output.Depths} in \code{simstrat.par}), so -- exactly as in the netCDF the
\code{.dat} files are otherwise converted to -- depth profiles share one \code{z}
grid, taken from the first depth-varying file. AED sediment-zone output
(\verb{<var>_zone_out.dat}) is on its own zone axis instead, and is returned as
an \code{\link{new_grouped_var}} object, again matching
\code{\link{read_simstrat_output}}.

Note that this needs the \code{.dat} files to still be there: they are kept by
default, but \code{\link{write_simstrat_nc}}\code{(remove_dat = TRUE)} deletes
them once they have been written to \code{output.nc}.
}
\seealso{
\code{\link{read_simstrat_output}} to read the same output back
from \code{output.nc}, \code{\link{write_simstrat_nc}} to produce that file,
and \code{\link{read_simstrat_dat_file}} for the single-file primitive.
}
