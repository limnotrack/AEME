% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_glm_outflows.R
\name{set_glm_outflows}
\alias{set_glm_outflows}
\title{Set outflow data for a GLM-AED simulation directory}
\usage{
set_glm_outflows(
  path_glm,
  outf,
  heights_wdr,
  outlet_type = NULL,
  flt_off_sw = NULL,
  bathy = NULL,
  dims_lake = NULL,
  wdr_factor = 1,
  glm_file = find_glm_nml(path_glm)
)
}
\arguments{
\item{path_glm}{filepath; to GLM-AED directory (containing the nml file
and a \verb{bcs/} subdirectory)}

\item{outf}{named list of data.frames, one per outflow, each with a
\code{Date} column and a flow column (\code{HYD_flow}, or \code{outflow} for a
\code{"wbal"} water-balance outflow) -- see \code{\link[=add_outflows]{add_outflows()}} for the expected
schema.}

\item{heights_wdr}{named numeric vector; outlet elevation (m) for each
name in \code{outf}.}

\item{outlet_type}{named numeric vector; GLM outlet type per outflow
(see the GLM manual). Defaults to \code{1} (fixed height) for every outflow.}

\item{flt_off_sw}{named logical vector; floating offtake switch per
outflow. Defaults to \code{FALSE} for every outflow.}

\item{bathy}{data.frame with \code{elev}/\code{area} columns describing the lake
hypsograph, used to size the outlet. Defaults to the \code{H}/\code{A} arrays
already in the GLM nml's \verb{&morphometry} block.}

\item{dims_lake}{numeric vector of length 2, \code{c(basin_length, basin_width)} at the crest. Defaults to the \code{bsn_len}/\code{bsn_wid} values
already in the GLM nml's \verb{&morphometry} block.}

\item{wdr_factor}{numeric; scaling factor applied to all outflow flow
rates. Default is \code{1}.}

\item{glm_file}{filepath; path to the GLM hydrodynamic nml file to
update. Defaults to the file found in \code{path_glm} via \code{\link[=find_glm_nml]{find_glm_nml()}}.}
}
\value{
invisibly, the updated nml object
}
\description{
Thin, \code{aeme}-free wrapper around the internal outflow writer used by
\code{\link[=build_aeme]{build_aeme()}}. Writes one boundary-condition csv per outflow into
\code{path_glm/bcs} and updates the \verb{&outflow} block of the GLM nml file.
Basin geometry (\code{bathy}, \code{dims_lake}) needed to size the outlets is read
from the existing \verb{&morphometry} block by default, so an existing
GLM-AED configuration can have its outflows edited without a lake
shapefile.
}
\examples{
\dontrun{
set_glm_outflows(path_glm, outf = list(outlet_1 = outflow_df),
                 heights_wdr = c(outlet_1 = -2.5))
}
}
